# TR\-RD28\&\&YL\-R1D机器人系统控制器开发报告

# 摘要

本文是 TR\-RD28\&\&YL\-R1D 复合移动机器人（双机械臂、全向四轮底盘、升降躯干，共 30 个关节）控制系统（YLR1D）的开发报告，供后续开发与维护人员使用。

系统基于 ROS2 Humble 与 Gazebo Classic 构建，包含 12 个功能包，覆盖模型资产、仿真、感知、控制、转译、规划、决策、人机界面与测试验证的完整软件栈。本文内容自包含：依次介绍系统总体框架、开发环境与构建方法、各子系统的结构 / 接口 / 关键机制、测试体系、已知问题与风险。附录 A 记录厂家原始开源项目的整理与加工过程，附录 B 记录 SDK 封装为 ROS2 节点并接入本系统的实测结果，供真机对接时参考。

# 1 概述

## 1.1 系统简介

YLR1D 控制系统面向一台复合移动机器人：底盘为四轮全向（4 个转向关节、4 个驱动轮），躯干为 4 关节升降机构，双臂各 7 个旋转关节并带 2 指夹爪，全模型共 30 个受控关节、27 路传感器。系统在 Gazebo 中完成运动学与动力学仿真，控制链路自上而下分层解耦。

关键指标如下：

1. 功能包数量：12 个（功能层 8 包：决策、规划双包、转译、控制、算法、仿真、感知；支撑包 4 个：模型资产、人机界面、启动编排、测试）；
2. 关节数量：30 个（转向 4、车轮 4、躯干 4、左臂 9、右臂 9）；
3. 传感器路数：27 路（3 台相机共 21 路、雷达 1、超声波 4、IMU 1）；
4. 自建接口：消息 / 服务 / 动作共 20 余个（算法层 10 消息 + 1 服务、感知层 2 消息、转译层 3 动作、规划层 4 消息 + 1 动作、决策层 4 动作）；
5. 自动化测试：16 项，覆盖环境检查、静态冒烟、仿真集成、故障注入与端到端 mock，结果落盘可追溯；
6. 节点运行形态：功能相对独立的部分以独立可执行节点运行，关联紧密的部分以 composition 组件形态运行。

## 1.2 分层架构

控制系统自顶向下分为决策、规划、转译、控制、算法、仿真层、仿真系统（Gazebo）、感知八个层级，各层职责与输入输出如下：

| 层级 | 职责 | 输入 | 输出 | 对应的包 |
|------|------|------|------|------|
| 决策层 | 以行为树编排任务，向规划层下发目标 | 任务描述 | 可规划的目标 | ylr1d_decision |
| 规划层 | 调用 nav2 与 MoveIt2 完成导航与机械臂规划 | 目标位置、位姿 | 封装的动作序列 | ylr1d_plan_nav、ylr1d_plan_moveit |
| 转译层 | 与厂家 SDK 接口对齐，把动作指令解算为关节期望 | 动作序列 | 关节期望值 | ylr1d_translate |
| 控制层 | 采样保持与信息调度，封装反馈 | 期望值、算法输出 | 管理 / 控制信息 | ylr1d_control |
| 算法层 | 维护控制律，计算加速度输出 | 期望、反馈 | 控制律输出 | ylr1d_algorithm |
| 仿真层 | Gazebo 与 ros2_control 的配置与启动，经 `plant_sim` 把控制指令积分为控制器命令 | 控制命令 | 仿真控制器命令 | ylr1d_plant |
| 仿真系统 | Gazebo 仿真环境 | 控制器命令 | 与环境交互的结果 | — |
| 感知层 | 接收、清洗、高级处理、分发传感器数据 | 各传感器原始数据 | 同步的、清洗后的、可靠的数据结果 | ylr1d_perception |

另有四个支撑包不参与上述层级：
- `ylr1d_hmi` 提供人机交互界面；
- `ylr1d_description` 提供模型与全局配置资产的单一来源；
- `ylr1d_test` 提供功能测试体系；
- `ylr1d_bringup` 提供一键启动编排。

十二个包的职责与相互关系的完整说明见第 3 至 13 章，系统分层架构与包间关系总览见图 1-1。控制系统框架代码仓库：[Zhengshuji/YLR1D_Controller](https://github.com/Zhengshuji/YLR1D_Controller)（分支 `model_fix`）。

```mermaid
flowchart TD
MAN[外部指定任务] --> |任务模式| DEC
subgraph 控制器
    DEC[决策层<br/>ylr1d_decision] --> |编排后的任务目标| PLN[规划层<br/>ylr1d_plan_nav<br/>ylr1d_plan_moveit]
    PLN -->|规划后的指令| TRL[转译层<br/>ylr1d_translate]
    TRL -->|解包后的期望| CTL[控制层<br/>ylr1d_control]
    CTL --->|控制器调用、维护| ALG[算法层<br/>ylr1d_algorithm]
    ALG -->|控制律输出| CTL
    ALG ~~~ CTL
    CTL -->|控制信息| PLT[仿真层<br/>ylr1d_plant]
    PLT -->|仿真控制信息| GZ[仿真系统<br/>Gazebo]
    GZ -->|传感器信息| PER[感知层<br/>ylr1d_perception]
    PER -->|内环反馈信息| CTL
    PER -->|内环反馈信息| TRL
    PER -->|外环反馈信息| PLN
end

subgraph ylr1d_hmi人机交互接口
    HMI1[rviz2 监视界面] 
    HMI2[Qt5 控制界面]
end
    HMI2 -.-> |发送控制指令| 控制器
    控制器 -.-> |系统信息| HMI1

    DESC[ylr1d_description<br/>模型资产单一来源] -..->|提供模型等资产| 控制器 & ylr1d_hmi人机交互接口

```

*图 1-1 系统分层架构与包间关系总览*


## 1.3 数据流

系统有两条主要数据流：控制链路（指令自上而下）与反馈回路（数据自下而上）。

**控制链路**依次为：

1. 决策层接收任务，下发给规划层；
2. 规划层在决策层或 HMI 的驱动下，经动作接口把高级指令下发给转译层；
3. 转译层在规划层或 HMI 的驱动下，把动作指令解算为关节期望值，发布到控制层；
4. 控制层在转译层或 HMI 的驱动下，对期望值采样保持并进行前馈补偿，整理控制算法需要的数据信息，提供给算法层；
5. 算法层根据期望与反馈计算控制量，返回控制层；
6. 控制层把控制量转发到仿真层；
7. 仿真层对控制信息做一定程度的预处理，经命令话题写入 Gazebo 控制器。

控制链路的数据流见图 1-2。

```mermaid
flowchart LR
    DEC[决策层] -->|任务| PLN[规划层]
    PLN -->|动作序列| TRL[转译层]
    TRL -->|关节期望<br/>/desired_joint_states| CTL[控制层]
    CTL -->|组期望<br/>/ctrl/组/input| ALG[算法层]
    ALG -->|控制指令<br/>/ctrl/组/output| CTL
    CTL -->|控制指令<br/>/plant/组/cmd| PLT[仿真层]
    PLT -->|命令话题| GZ[Gazebo 控制器]
```

*图 1-2 控制链路数据流*

**反馈回路**依次为：

1. Gazebo 发布 30 关节全量状态与 27 路原始传感器信息；
2. 感知层接收层将传感器恒等转发至内部话题，并清洗关节数据、进行预处理；
3. 感知层高级感知层调用高级感知算法，实现高级感知功能；
4. 感知层分发层与消费方对齐、统一时效性，核查数据后统一发布。

目前消费方主要有：
1. 控制层以感知层测量驱动模型预测器，封装反馈供算法层闭环；
2. 27 路传感器经 `/perception/sensors/*` 对外分发，供 HMI 与 rviz 展示；
3. 规划层依赖感知层提供的信息。

反馈回路的数据流见图 1-3。

```mermaid
flowchart LR
    GZ[Gazebo] -->|30 关节状态<br/>27 路传感器| 接收层
subgraph 感知层
subgraph 接收层
    REC1[订阅接受] --> REC2[清洗过滤] --> REC3[预处理] --> REC4[恒等转发]
end
subgraph 高级感知层
    ADV1[订阅获取] --> ADV2[同步预处理] --> ADV3[高级感知算法调用] --> ADV4[结果发布]
end
subgraph 分发层
    DSP1[订阅获取] --> DSP2[信息核查] --> DSP3[同步处理] --> DSP4[统一发布]
end
    接收层 --> 高级感知层
    高级感知层 --> 分发层
    接收层 --> 分发层
end
    分发层 -->|/perception/joint_state| CTL[控制层<br/>模型预测器]
    CTL -->|反馈| ALG[算法层闭环]
    分发层 -->|/perception/sensors/*| HMI[HMI / rviz 监视]
    分发层 -->|高级感知信息| PLN[规划层]
```

*图 1-3 反馈回路数据流*

## 1.4 设计原则

系统遵循以下设计原则，后续扩展应沿用：

1. **算法与控制分离**：算法层只维护控制律（纯 C++ 库，使用统一的 ROS2 框架封装），控制层只做采样保持与信息调度，仿真层负责积分并与 Gazebo 对接。更换控制算法只需在算法层新增控制律并替换模板实参，调整控制器之间的组合关系，节点核心框架与控制层无需改动。
2. **反馈经模型预测器封装**：控制层以虚拟对象跟踪被控对象，利用感知层测量做校正，为算法层提供平滑、无延迟滞后的反馈，消除闭环系统中滞后环节的剧烈震荡，增强稳定性。
3. **运行期调参仅走服务**：控制器参数的默认值在算法层头文件中以编译期常量定义，运行期只允许经 `/ctrl/<组>/command` 服务修改，不提供 `ros2 param set` 通道，保证参数变更可审计。同时，适应参数自适应快速调整的情况。
4. **静态配置单一来源**：关节分组与限位、传感器话题、底盘运动学、命令话题、控制参数分别收敛于唯一配置头（见 3.4 节），修改一处全局生效，代价是须重新编译消费方。
5. **双轨运行形态**：算法、控制、转译、仿真四层均同时提供独立可执行与 composition 组件两种形态且节点名一致，launch 默认采用 composition，独立形态保留调试能力，依赖节点名的下游模块无需改动。
6. **工程化保障**：三层统一健康话题、控制信息断流急停、启动编排单实例互斥锁、固定时延分层启动、容器级看门狗重启、故障注入测试与 16 项自动化回归。

## 1.5 开发与扩展约定（总则）

设计阶段把内容分为两类：一类是**固定框架**——每层中的核心节点、层级交互接口与部署形态在设计中基本确定下来，建议后续开发参考遵守；另一类是**预留扩展点**——刻意留下的能力接口（如高级感知、控制律），"完善"它们属于能力扩展。本节给出跨层总体约定，各层具体的固定接口、节点骨架与扩展点见对应章节（索引见 1.5.3）。

### 1.5.1 分层与依赖方向（固定约定）

1. 八层级结构固定：决策、规划、转译、控制、算法、仿真、仿真系统（Gazebo）、感知；跨层交互只经约定接口，禁止跨层直达（允许设置直通通道）；
2. 感知层是公共数据源：感知层只依赖 description 配置（`joint_config.hpp` 等），算法、控制、转译、仿真、规划、决策各层对感知层有数据依赖，感知层不得反向依赖任何上层；
3. 指令链路逐层单向：决策 → 规划 → 转译 → 控制 ↔ 算法 → 仿真 → Gazebo。其中，转译层为上下层的分界点，新增上层能力不得绕过转译层直接下发关节指令（决策层边界约定见 10.1 节），替换下层能力需要与转译层兼容（详见附录B所示案例）；
4. 包名与核心节点名固定：算法、控制、转译、仿真四层双轨运行（独立可执行 / composition 组件）且节点名一致，依赖节点名的 launch、测试与 HMI 按名引用。

### 1.5.2 预留接口完善与缺陷改进的区分

**定义**：
- **缺陷改进**：既有已声明功能或机制不符合预期——bug、环境适配、鲁棒性缺口（断流、超时、启动时序、渲染崩溃等），修在既有框架内；
- **预留接口完善**：启用或充实设计期预留的能力点——新控制律、新高级感知节点、多路同步、新任务类型等，属能力扩展。

**判定原则**：先问"改的是框架，还是框架内的实现 / 参数"。接口（话题 / 动作 / 服务名与语义）、节点骨架、分层边界与 1.5.1 所列约定变动 = 框架变更，须评审并同步文档；框架内的新实现（新控制律类、新 `advanced` 节点、新行为树 XML）或参数调整 = 扩展或调参，不进框架评审通道。

**执行纪律**：缺陷改进采用最小改动并同步对应层测试断言与回归验证，不得借机重设计框架；预留接口完善须明确新增内容、兼容性影响与测试补充后实施，不得伪装成缺陷修复混入；对外接口扩展（新话题 / 动作 / 服务）须登记并同步本章与对应章节。

**文档同步**：任何框架或约定变更须同步本章、对应功能包章节与各包 README；修改 launch / 配置 / Python 模块后须重新构建安装对应包（见 2.3 节），避免运行时加载旧副本。

**示例对照**（同一系统内两类工作的典型例子）：
- 缺陷改进：轮式里程计解算矩阵奇异加正则项（见 8.3 节）、软件渲染下 rviz2 崩溃禁用相关显示项（见 14.2 节）、启动时序与发现机制问题（见 14.1 节）——均修在既有框架内；
- 预留接口完善：新增 MPC / 自适应控制律（见 5.8 节）、视觉里程计与目标检测（见 8.8 节）、多路时间同步实例化（见 8.8 节）、抓取序列任务（见 10.8 节）、边转边平移分段策略（见 7.7 节）——均为能力扩展。

### 1.5.3 各层固定框架与扩展点索引

下表按功能包给出各层**核心节点**（该层最核心、通常直接运行的节点，决定了这一层的架构形态）、**固定框架（契约）**与**预留扩展点**：

| 章节 | 核心节点 | 固定框架（契约） | 预留扩展点 |
|------|----------|------------------|------------|
| 3 章（description） | 无（模型资产与全局配置单一来源，无运行期节点） | 静态配置单一来源（关节分组 / 限位、传感器话题、底盘运动学） | 新增传感器（三处同步）、新增控制组 / 关节 |
| 4 章（plant） | `plant_sim`（仿真组件）、`robot_state_publisher`（rsp） | 双仿真方案、五组命令话题、断流急停 E_STOP | 事件驱动启动时序、碰撞原语更换（见 4.8 节） |
| 5 章（algorithm） | `steering_sim` / `wheels_sim` / `torso_sim` / `left_arm_sim` / `right_arm_sim`（五组纯控制器节点） | 六接口契约、节点骨架（输入 → 组合控制器 → 输出加速度）、调参仅走服务 | 新增控制律（`ControllerT<LawT>` 换模板实参） |
| 6 章（control） | `chassis_control`、`arm_control` | `GroupForwarder` 转发器骨架 | 校正系数运行期可调、按组分错峰转发（见 6.6 节） |
| 7 章（translate） | `translate_server` | 三动作接口、先转向后移动两阶段、期望合并发布 | 边转边平移分段策略（见 7.7 节） |
| 8 章（perception） | 接收层六类（image / camera_info / point_cloud / laser_scan / imu / joint_state receiver）、`joint_state_estimator`、`wheel_odometry`、`health_aggregator`；分发层 `control_feedback_guard`、`sensor_dispatch` | 接收层模板、分发层"骨架 + 同步单元"、内外部话题前缀隔离 | 高级感知节点（`advanced/` 目录）、多路同步实例化（见 8.8 节） |
| 9 章（plan） | `cmd_vel_bridge`（导航）；`moveit_bridge`、`moveit_goal_server`（机械臂）；nav2 / move_group 为外部依赖 | 规划层统一框架、`cmd_vel_bridge` 只做通信 | 分段策略替换（`command_converter`）、求解器 / 规划器更换（见 9.4 节） |
| 10 章（decision） | `mission_server`、`decision`、`plan_client`（三层三节点） | 三层三节点、行为树引擎 | 新任务 XML、新叶子节点类型注册 |
| 11 章（hmi） | 九个面板程序：`ylr1d_hmi_control` / `ylr1d_hmi_translate` / `ylr1d_hmi_sensor`（含 `_direct` 直连版）/ `ylr1d_hmi_algorithm` / `ylr1d_hmi_plan` / `ylr1d_hmi_moveit` / `ylr1d_hmi_decision` / `ylr1d_hmi_monitor` | 单线程模型、监视面板静态结构 + 动态探查、健康解析统一 | 指令持久化、界面线程拆分（见 11.7 节） |
| 12 章（bringup） | 无（纯启动编排，不含节点） | 固定时延分层启动、互斥锁、参数改名隔离 | 事件驱动时序评估（见 12.6 节） |
| 13 章（test） | 无（测试工具包，`run_tests.sh` 统一调度） | 独立 launch、证据链分层诊断 | CI 接入、真机回归（见 13.7 节） |

## 1.6 文档结构

本文按以下顺序组织：第 1 章概述系统并给出开发与扩展的总体约定（1.5 节）；第 2 章为开发环境与构建方法；第 3 至 13 章逐层介绍各功能包，每章末尾统一给出该部分的使用限制、现存问题与后续规划；第 13 章为测试体系；第 14 章汇总系统级问题与风险。
# 2 开发环境与构建

## 2.1 运行环境

系统在以下环境中开发并验证：

1. 操作系统：WSL Ubuntu 22.04（x86_64），原生 Ubuntu 22.04 亦可；
2. ROS 发行版：ROS2 Humble；
3. 仿真环境：Gazebo Classic 11；
4. 构建工具链：colcon、ament_cmake、CMake、g++（C++17）；
5. 界面与显示：Qt5（HMI 面板）、rviz2（显示与插件）；
6. 数值与算法库：Eigen（随工作区第三方目录提供，算法层使用）、behaviortree_cpp_v3（决策层，与 nav2 同款依赖）；
7. 规划依赖：nav2 各模块（Humble 发行包）、MoveIt2（moveit_msgs、moveit_ros_planning_interface、kdl_kinematics_plugin）；
8. 语言约定：节点与核心逻辑全部使用 C++，Python 仅用于 launch 与测试层。

## 2.2 环境变量

以下环境变量由各 launch 自动注入，单独启动命令行、脚本或 GUI 程序时需要自行设置，否则行为不符合预期：

1. `ROS_LOCALHOST_ONLY=1`：将节点限制在回环网络 DDS 组内，规避多播发现缓慢的问题，使同步服务调用立即响应。所有 bringup 顶层注入；单独启动的任何 ROS2 进程必须设置同一值，否则与 bringup 树内节点互相不可见；
2. `LIBGL_ALWAYS_SOFTWARE=1`：强制软件渲染，用于 WSL 下无 GPU 直通的图形程序（rviz2、Gazebo 界面），避免因设备无 GPU 直通导致渲染出错；
3. `GAZEBO_MODEL_PATH`：追加模型库目录，使 `model://ylr1d_description/...` 资源引用可解析，由仿真 launch 自动注入；实际实现中改用 Python 函数手动注入模型库路径，不再依赖该环境变量的配置。
4. `YLR1D_DISABLE_CAMERAS=1`：把相机类传感器更新频率置零，Gazebo 停止渲染相机，显著降低仿真 CPU 占用。规划导航与决策演示的 bringup 注入该变量，感知层图像 / 点云话题因此无数据，属预期现象；需要视觉功能时去掉该注入。

## 2.3 构建方法

全量构建：

```bash
cd <工作空间根目录>
colcon build
source install/setup.bash
```

构建与安装须遵守以下重编规则，否则运行时加载的是安装目录中的旧副本，表现为"源码正确但行为不对"：

1. 修改 launch 文件、yaml 配置或 Python 模块后，必须重新构建并安装对应包（例如 `colcon build --packages-select ylr1d_bringup`）。排查方法：对比安装目录 `install/<包>/share/<包>/launch/` 与源码 `src/<包>/launch/` 是否一致，Python 模块则对比 `install/<包>/lib/python3.10/site-packages/`；
2. 修改单一来源配置头（`joint_config.hpp`、`sensor_topics.hpp`、`chassis_kinematics.hpp`、算法层 `Params`）后，必须重新编译全部消费方（见 3.4 节）；
3. 修改 CMakeLists 的 install / export 配置后，若增量构建未重新配置，应删除对应包的 `build/<包>` 与 `install/<包>` 目录强制重配；
4. 消费方链接算法层核心库必须使用命名空间目标 `ylr1d_algorithm::ylr1d_algorithm_core`（原因与约定见 5.5 节）。

## 2.4 启动与运行验证

系统提供五套一键启动链路（详细时序见 12.2 节）：

```bash
ros2 launch ylr1d_bringup bringup_control.launch.py        # 控制闭环链路
ros2 launch ylr1d_bringup bringup_translate.launch.py      # 转译链路（动作驱动）
ros2 launch ylr1d_bringup bringup_plan_nav.launch.py world:=nav_test.world   # 导航链路
ros2 launch ylr1d_bringup bringup_plan_moveit.launch.py    # 机械臂规划链路
ros2 launch ylr1d_bringup bringup_decision.launch.py world:=nav_test.world   # 决策层全栈
```

启动后的验证步骤：

1. 终端设置 `export ROS_LOCALHOST_ONLY=1`，使命令行与节点处于同一 DDS 组；
2. 轮询 `ros2 control list_controllers`，直到 6 个控制器全部为 active。WSL 下 Gazebo 加载需要 30 至 60 秒，不得提前下发命令；
3. 确认 `/joint_states` 有数据（30 关节）；
4. 转译链路确认 `ros2 action list -t` 出现 `/chassis_move`、`/arm_move`、`/gripper_move` 三个动作服务；
5. 使用 rqt 工具查看各节点是否全部启动。

## 2.5 调试手段

1. 数据流验证优先使用 rclpy 脚本：系统负载较高时，`ros2 topic echo`、`ros2 param get` 等命令行工具可能返回空结果或上下文错误（执行 `ros2 daemon stop` 可恢复），rclpy 测试节点不受影响；
2. 排查"已发布但无人接收"时，使用 `ros2 topic info -v <话题>` 查看发布端与订阅端计数，比查看日志更快；
3. 演示与测试之间使用一键清场工具 `stop_all.sh` 清理残留进程（Gazebo、容器、rviz2、规划栈等），避免残留进程干扰下一次启动；
4. 可以使用 hmi 提供的人机交互工具，进行监视、检验。
# 3 ylr1d_description — 模型资产与全局配置

## 3.1 功能定位

`ylr1d_description` 是模型资产与全局配置的单一来源，以功能包形式承载 xacro 模板、STL 网格、模型参数、仿真世界、显示配置与跨包 C++ 配置头。该包无运行期节点、不收发话题，只提供资产与编译期接口。仿真层与各展示程序均从本包取资产，保证模型全局的一致性。全模型 30 个受控关节的分组关系见图 3-1。

```mermaid
flowchart TD
    BASE[Link_Base 车体] --> STEER[转向组 steering<br/>4 关节 position]
    BASE --> WHEEL[车轮组 wheels<br/>4 关节 velocity]
    BASE --> IMU[imu传感器] & Radar[雷达传感器] & Ray[4 超声波传感器]
    BASE --> TORSO[躯干组 torso<br/>4 关节 position] --> TORSOCAM[全局相机]
    TORSO --> LARM[左臂组 left_arm<br/>7 关节 + 2 夹指] --> LARMCAM[左臂相机]
    TORSO --> RARM[右臂组 right_arm<br/>7 关节 + 2 夹指] --> RARMCAM[右臂相机]

```

*图 3-1 模型树与关节分组*

此外，本包通过 launch 提供基础的模型查看、传感器功能演示与关节控制演示功能。

## 3.2 包结构

本包以目录形式组织模型资产与配置，结构如下：

```text
ylr1d_description/
├── urdf/          基于 xacro 的模型文件
├── config/        yaml 格式的模型参数文件
├── meshes/        36 个 STL 网格
├── worlds/        Gazebo 世界模型配置
├── maps/          导航测试地图配置
├── rviz/          rviz2 配置
├── launch/        演示文件 launch + 通用 python 工具
├── include/ylr1d_description/config/  三个 C++ 配置头（见 3.4 节）
└── model.config   Gazebo 模型库描述
```

## 3.3 模型资产

1. `urdf/ylr1d.xacro` 为模型宏，通过 `${参数名.键}` 形式引用 config 目录下 yaml 中的参数，定义 30 个受控关节（全部走 `gazebo_ros2_control/GazeboSystem`）与 15 个 Gazebo 传感器块（3 台相机、雷达、IMU、4 个超声波）。关节命令接口划分：底座转向、躯干关节组、双臂关节组、夹指为 position 接口，底座车轮为 velocity 接口；力控方案统一为 effort 接口；
2. `urdf/ylr1d_sensor_vis.xacro` 与正式模型除传感器可视化开关外一致，供诊断时查看传感器朝向与位置，也用于可视化演示；
3. `config/*.yaml` 将几何、惯量、限位、传感器等参数外置于 xacro 模型之外，修改参数无需改动 xacro 文件；
4. TF 坐标系约定：模型根帧为 `Link_Base`（`base_footprint` 虚拟连接已注释）；导航场景中 `map → odom → Link_Base` 链由感知层定位（amcl + EKF）建立。

## 3.4 全局配置头（跨包单一来源）

以下三个 C++ 头文件被多个包编译期引用，是全项目静态配置的单一来源：

1. `joint_config.hpp`：包括关节分组表 `kJointGroups`（steering、wheels、torso、left_arm、right_arm 五组）与限位查表 `jointLimitFor`。算法层、控制层、仿真层、感知层均引用，修改后须重新编译全部消费方；
2. `sensor_topics.hpp`：包括 27 路传感器的话题名、频率与参考坐标系规格表。感知层接收节点与 HMI 引用，新增传感器只改此处；
3. `chassis_kinematics.hpp`：包括底盘运动学常量（轮位、轮距、原地旋转与停车转向角、速度换算比）。转译层三模式与感知层轮式里程计共用，修改后须重编这两层。

**扩展约定**：新增控制组 / 关节经 `joint_config.hpp` 分组注册表与限位表扩展（配套算法层节点、控制层转发器与仿真层控制器实例化）；新增传感器须同步维护 `sensors.yaml`（Gazebo 块）、`sensor_topics.hpp`（规格表）与诊断副本 xacro 三处（见 3.6 节）。此类扩展属预留接口完善（见 1.5.2 节），修改单一来源后须重编消费方。

## 3.5 xacro 展开公共管道

由于在ROS2中， xacro 宏不能直接、自动地加载 yaml 参数，因此把"yaml 引用替换 + xacro 展开"收敛为公共管道 `launch/python_utils/xacro_utils.py`，展示与仿真层走同一条生成路径，保证各处 URDF 一致。管道执行过程：先以正则预替换 `${参数名.键}` 为 yaml 字面量，再注入控制器配置文件路径与可选转换链，最后调用 xacro 展开并写入临时文件（程序退出时清理）。

## 3.6 限制与问题

1. 包内不提供落盘的 URDF 产物，展示与仿真都在运行时生成临时文件，MoveIt 等所需的 URDF 配置需由规划层脚本从 xacro 现场生成（见 9.2.2 节），增加了离线检查的间接性；
2. 新增传感器需要同时维护 `sensors.yaml`（Gazebo 块）、`sensor_topics.hpp`（规格表）与诊断副本 xacro 三处，改动点较多；
3. `scale.yaml`（密度）参数当前未被 xacro 引用，修改不生效，属遗留参数；其最初设想是批量调整各部件的质量与惯量。
4. 部分 rviz2 显示配置依赖自建插件，这些插件位于 ylr1d_hmi 包中。
5. 模型资产发生修改时，整个包的改动点较多，主要原因是SolidWorks导出urdf模型存在固有问题，以及转换工作流不完善导致的。（详见附录A相关内容）

## 3.7 后续规划建议

1. 收敛新增传感器的改动点，将传感器描述进一步参数化，减少多文件同步；
2. 接入密度参数或清理遗留的 `scale.yaml`；
3. 通过实际的数据，固定化 xacro 模型得到稳定的 URDF 模型，提升系统运行效率。

# 4 ylr1d_plant — 仿真层

## 4.1 功能定位

`ylr1d_plant` 是系统在 Gazebo 中的仿真层，管理 Gazebo 与 ros2_control 的配置和启动，并通过仿真组件 `plant_sim` 把控制层转发的控制指令转换为 Gazebo 控制器能够处理的内容，再写入 Gazebo 仿真环境。本包管理仿真系统相关内容。仿真场景中机器人整体（双机械臂、全向四轮底盘、升降躯干）与传感器可视化（相机视锥与点云、雷达扫描线、IMU、超声波）的效果见图 4-1。

![图 4-1 Gazebo 仿真场景与传感器可视化](images/gazebo-sensors-monitor.png)

*图 4-1 Gazebo 仿真场景与传感器可视化*

## 4.2 双仿真方案

| 方案 | launch | 控制器配置 | 当前运行形态 |
|------|--------|-----------|----------|
| position（默认，运动学仿真） | `gazebo_position.launch.py` | `controllers.yaml` | 无界面 gzserver，rsp 与 plant_sim 装入 composition 容器；命令话题 `*_controller/commands` |
| effort（可扩展，动力学仿真） | `gazebo_effort.launch.py` | `effort_controllers.yaml` | 带界面 gazebo，rsp 为独立节点、无 plant_sim；命令话题 `*_effort_controller/commands` |

两方案基于同一份 xacro 资产，控制器配置文件的路径由各自 launch 注入；力控方案另向 ros2_control 声明 effort 命令接口。考虑到SDK包通常不需要自行完成动力学建模与解算，闭环演示仅使用 position 方案，但保留 effort 方案便于扩展。

## 4.3 控制器与命令话题

Gazebo 中加载 6 个 ros2_control 控制器：1 个关节状态广播器与 5 个组命令控制器。命令话题与关节对应关系如下：

1. `chassis_steering`：4 个转向关节（position）；
2. `chassis_wheels`：4 个驱动轮（velocity）；
3. `torso`：4 个躯干关节（position）；
4. `left_arm` / `right_arm`：各 9 个（7 个臂关节 + 2 个夹指，position）。

## 4.4 接口

**输入**：

1. `/plant/<组>/cmd`（5 组，`std_msgs/Float64MultiArray`）：控制层加速度命令，组件对命令采样保持并将非有限值置零；
2. `/perception/sensors/joint_states`（`sensor_msgs/JointState`）：感知层清洗后的关节状态，作为 plant_sim 的初始化源与 rsp 的 TF 数据源；
3. `*_effort_controller/commands`（力控方案）：直接以话题发布的力矩命令，不经仿真组件。

**输出**：

1. `/joint_states`（`sensor_msgs/JointState`）：30 关节全量状态（位置 / 速度 / 力矩），由关节状态广播器发布；
2. `*_controller/commands`（position 方案）：plant_sim 积分后的组命令；
3. `/health/plant_sim`（`std_msgs/String`，500 ms）：仿真层健康状态，取值 OK、AWAITING_FEEDBACK、AWAITING_ACCEL:<组>、E_STOP:<组>。

## 4.5 关键机制

1. **plant_sim 仿真器**：控制层输出的是加速度语义，而 Gazebo 命令控制器需要位置 / 速度命令，两者语义不同，需要转换，plant_sim 正是为弥补这一接口差异而存在的仿真器。plant_sim 目前直接对加速度积分，并对积分结果做限幅处理，相当于实现加速度到位置的仿真：位置组按"加速度 → 速度限幅 → 速度积分 → 位置积分 → 位置限位"处理，速度组按"加速度 → 速度限幅 → 速度积分"处理。特别地，算法核心 `SimPlant` 是零 ROS 依赖的纯算法类，与被控对象模型解耦，更换模型不动通信代码。初始化使用感知层关节状态首帧，组内任一关节命中即整组初始化；
2. **断流急停（E_STOP）**：任一组加速度断流超过 500 ms，视为控制链路失效，位置组保持当前位置、车轮速度归零并直接下发安全命令，同时经健康话题上报；加速度流恢复后自动恢复积分。从未收到命令的组上报 AWAITING_ACCEL（控制层未启动，非异常），未完成初始化的组上报 AWAITING_FEEDBACK；
3. **composition 容器**：rsp 与 plant_sim 装入同一容器共享进程（同一数据源、与 Gazebo 生命周期对齐），容器级看门狗在组件崩溃后自动重启并重载全部组件；
4. **启动时序与清理**：spawn 过程带重试循环，控制器 spawner 延迟启动并设 90 秒超时；启动前清理残留 gzserver 进程，退出时清理并删除临时 URDF，防止二次启动报实体已存在；
5. **DDS 组注入**：launch 双层注入 `ROS_LOCALHOST_ONLY=1`，保证 gzserver、容器、spawner、rviz 同处回环 DDS 组（见 2.2 节）。

## 4.6 验证

1. 仿真层集成测试：启动 Gazebo 后断言 6 个控制器全部 active、`/joint_states` 有 30 关节数据、gzserver 进程存活；
2. 故障注入测试（显式运行）：注入 E_STOP 三态（等待加速度 → 正常运行 → 断流急停，断言车轮归零 / 位置组冻结）、加速度断流停转、健康话题停止发布、容器组件崩溃后 respawn 恢复；
3. 闭环验证：随 `bringup_control` 完整闭环运行，全流程测试以"6 控制器 active、`/joint_states` 有数据"作为仿真层证据。

## 4.7 限制与问题

1. WSL 下控制器从启动到 active 需要 30 至 60 秒，且必须先启动感知层（数据源为感知层清洗话题），单独启动本层时 rsp 无 TF、plant_sim 不下发命令；
2. 力控方案目前仅验证了力矩控制可用。
3. 加速度限幅为运动学量，不代表 Gazebo 电机的物理极限。

## 4.8 后续规划建议

1. 评估以事件驱动替代固定时延的启动时序，缩短慢场景下的就绪时间；
2. 换用更稳健的碰撞原语改善轮子接触模型（与导航精度问题相关，见 14.2 节）。
3. 进一步完善、提升系统的鲁棒性，完善例如看门狗等内容。

# 5 ylr1d_algorithm — 算法层

## 5.1 功能定位

`ylr1d_algorithm` 是控制链路的算法层，以接口包形式承载控制律：核心算法封装为零 ROS 依赖的纯 C++ 库；另按控制层的要求提供 5 个纯控制器节点（负责订阅输入与反馈、计算并发布输出、维护自身参数与状态），并自建 10 个消息与 1 个服务供层间通信。算法层只负责控制律的维护与管理，不持有被控对象（被控对象模型位于仿真层）。

## 5.2 核心库设计

核心库由三层抽象构成：

1. **统一控制器接口 `Controller`**：定义 `compute(input, feedback, dt)`、参数 / 状态管理方法集（set_param、get_param、set_state、get_state、reset_state、initialize）与维度查询。多数控制算法只需"输入、反馈、离散时间"即可完成计算与自适应调节。特别地，input、feedback 统一为 Eigen 向量类型的数据，如果控制律对数据的要求较为复杂，可以对数据进行统一的向量化操作，并在内部进行分割、重组等操作还原；
2. **控制单元模板 `ControllerT<LawT>`**：把满足约定（参数类型 + 配置方法 + 统一方法集）的控制律适配为 Controller 类，便于批量复用。更换算法即新增一个控制律类并替换模板实参，节点框架与层间通信零改动；
3. **PID 控制律 `PidLaw`**：标量 kp / ki / kd / 输出限幅，基于 Eigen 向量化实现，当前为唯一内置控制律。特别地，这个类也可以单独实现比例、微分（差分）、积分（求和）的功能。

五组节点在单元之上组合为**组合控制器**：每组负责一个完整、独立的被控对象，组内被控量之间通常存在较强耦合、无法进一步拆分。

> 在本项目中，使用这样的组合方式作为案例：
> 1. 位置组先经比例系数为 1 的控制器解耦，得到各被控对象实际需要的控制量；
> 2. 各控制量再送入各自的 PID 控制器，微分项使用反馈速度做阻尼（`-kd·v`）；
> 3. 最终结果重新打包发送；速度组直接采用默认计算路径。

**控制律扩展约定**：新增控制律 = 新建满足约定（参数类型 + 配置方法 + 统一方法集）的控制律类，并用 `ControllerT<新控制律>` 替换对应组的模板实参；节点框架、层间通信与控制层零改动，`PidLaw` 可作参照实现。新增控制律属预留接口完善（见 1.5.2 节），与缺陷改进区分。

控制器装配关系见图 5-1。

```mermaid
flowchart TD
    subgraph CORE[核心库]
        IFC[统一接口 Controller] --> TPL[单元模板 ControllerT]
        TPL --> PID[控制律 PidLaw<br/>Eigen 向量化]
    end
    IN[输入：期望 / 反馈 / 反馈速度] --> COM[组合控制器<br/>五组同构节点]
    COM --> FW[前馈通道 whole<br/>位置组 P=1 解耦]
    FW --> JP[逐关节 PID<br/>joints / gripper 寻址]
    JP --> OUT[输出：加速度 u]
    COM -. 基于 .-> TPL
```

*图 5-1 算法层控制器结构*

## 5.3 参数路由与调参机制

组合控制器内部含多个子控制器，服务调参按子控制器标识路由，参数键形如 `controller.<子控制器标识>.<参数>`。
> 当前参数键分为以下四类：
> 1. `whole`：位置组前馈通道（速度组无）；
> 2. `joints`：逐关节 PID，支持 `joints_list` 广播与 `joints_<关节序号>` 单关节覆盖两种寻址；
> 3. `gripper`：臂组夹爪（关节序号 7、8）的 PID；
> 4. 裸键 `controller.output_limit` 为组合控制器自身的输出限幅。

## 5.4 节点与接口

五组节点（steering、wheels、torso、left_arm、right_arm）结构同构，固定执行"输入（期望、反馈、反馈速度）→ 组合控制器 → 输出加速度"的处理流程。节点与组的对应关系：

- steering：位置组，4 关节；
- wheels：速度组，4 关节；
- torso：位置组，4 关节；
- left_arm / right_arm：位置组，各 9 关节（含夹指）。

**输入**：

1. `/ctrl/<组>/input`（`Float64MultiArray`）：组期望（位置组为期望位置，速度组为期望速度）；
2. `/ctrl/<组>/feedback`（`JointGroupEstimate`）：控制层模型预测器封装后的反馈状态；
3. `/ctrl/<组>/command`（服务请求）：调参指令，支持 get_param、set_param、get_state、set_state、reset_state、initialize 六种。

**输出**：

1. `/ctrl/<组>/output`（`Float64MultiArray`）：加速度数组，维度 4 / 4 / 4 / 9 / 9；
2. `/ctrl/<组>/param`、`/ctrl/<组>/state`：控制器的参数与状态只读展示，10 Hz；
3. `/ctrl/<组>/command`（服务响应）：调参结果。

**输出限幅策略**：加速度输出按 `controller.output_limit` 限幅，默认值低于物理限幅（位置组 2.0、夹爪 0.5、车轮 5.0，对应物理基准 5.0 / 1.0 / 10.0）。原因是仿真层不对加速度命令做二次钳制，若限幅等于物理值，大误差时输出将直接饱和并进入极限环，表现为关节剧烈震荡。

**初始化时序**：节点只等待首帧期望即完成初始化（反馈默认取期望、误差为零），不依赖反馈首帧，脱离感知层与仿真层也能开环输出，避免启动顺序死锁；真实闭环由控制层接入感知层测量后形成。

**接口与骨架约定**：六接口（`/ctrl/<组>/{input,feedback,output,param,state}` 与 `/ctrl/<组>/command` 服务）为算法层与控制层之间的固定契约，话题名、消息类型与语义不得随意变更；节点固定执行"输入（期望、反馈、反馈速度）→ 组合控制器 → 输出加速度"框架，只等首帧期望完成初始化、不自行回环。新增控制组须同步更新 `joint_config.hpp` 分组注册表（见 3.4 节），并实例化对应节点与控制层转发器。

## 5.5 构建与链接约定

1. 核心库导出 CMake 目标 `ylr1d_algorithm::ylr1d_algorithm_core`，消费方必须链接命名空间目标。若只导出链接名、以裸库名链接，会丢失头文件目录与编译特性（典型报错为找不到核心库头文件），正确做法是 `install(TARGETS ... EXPORT <名>)` 与 `ament_export_targets(<名> HAS_LIBRARY_TARGET)` 成对使用；
2. 接口包构建约定：rosidl 生成目标必须与工程名一致（运行期按消息包名定位类型支持库）；同包节点链接自建消息须经 `rosidl_get_typesupport_target` 获取实际接口库；头文件目录导出须使用带尾部斜杠的安装路径，避免嵌套目录。

## 5.6 验证

1. 单元测试（零 ROS）：断言五组控制器计算输出、参数路由寻址与异常路径（未知参数键、维度错误返回失败与错误信息）；
2. 运行期调参验证：经 `/ctrl/<组>/command` 修改 kp / kd 等参数即时生效，HMI 算法层面板可观测参数与状态变化；
3. 闭环验证：随 `bringup_control` 完整闭环，算法层按"期望与模型预测器反馈"计算加速度，全流程测试覆盖。
4. 算法层 HMI 可直观观测五组控制器的运行情况（只读监视，见 11.2 节）。

## 5.7 限制与问题

1. 参数默认值为编译期常量，修改默认值需重编并重启节点；运行期仅服务调参，无 `ros2 param set` 通道（属设计约定）；
2. 期望与反馈消息不含时间戳，节点按最新帧处理；输入断流超过 500 ms 仅告警，断流防护依赖控制层；
3. composition 容器为多线程执行器，回调并发共享状态依赖互斥保护，回调内不得长时间阻塞。

## 5.8 后续规划建议

1. 引入更多控制律（MPC、自适应控制等），复用统一接口与单元模板；
2. 为期望与反馈消息补充时间戳，提升断流判定与时序分析的准确性。
3. 增加对算法层的时效性处理。目前，控制层与算法层基本保持频率一致、工作流程固定，但实际上算法层可以拥有更强的独立性。一方面，算法层的频率可以更高，在自适应控制等算法中有较大意义；另一方面，算法层的工作线程可以由算法层自行编排，可以大幅提升时效性。

# 6 ylr1d_control — 控制层

## 6.1 功能定位

`ylr1d_control` 是控制链路在下位机的核心与数据处理中枢，主要承担信息调度与系统管理的职责：订阅上层期望并做前馈补偿等处理；接收感知层数据并做相应处理；与算法层交互，按组发送输入与反馈、接收算法层输出；把控制信号转发给仿真层；内置错误处理机制，保证系统具备一定的鲁棒性。

本层含两个节点：`chassis_control`（steering、wheels 两组）与 `arm_control`（torso、left_arm、right_arm 三组），均支持独立可执行与 composition 组件两种形态。

## 6.2 接口

**输入**：

1. `/desired_joint_states`（`sensor_msgs/JointState`）：上层期望，按关节名对齐；位置组用 position 字段，车轮组用 velocity 字段；
2. `/ctrl/<组>/output`（`Float64MultiArray`）：算法层加速度输出；
3. `/perception/joint_state`（`JointStateEstimate`）：感知层五组位置 / 速度 / 加速度测量，是本层反馈源，驱动模型预测器校正；
4. `/control/<节点名>/command`（服务请求）：聚合调参入口，按 group 字段路由到对应组。

**输出**：

1. `/ctrl/<组>/input`（`Float64MultiArray`）：组期望，采样保持后发布；
2. `/ctrl/<组>/feedback`（`JointGroupEstimate`）：模型预测器状态（位置组位置与速度为模型值，速度组位置恒零、速度为模型速度，加速度取感知层值）；
3. `/plant/<组>/cmd`（`Float64MultiArray`）：加速度转发，断流或无订阅时不发布；
4. `/health/<节点名>`（`std_msgs/String`，500 ms）：健康状态，取值 OK、STALE:<组>(<秒>)、NO_CMD_SUB:<组>、AWAITING_ACCEL；
5. `/simulated_{chassis,arm}_states`：感知层位置采样保持，调试用。

**参数**：`loop_hz`（默认 100，采样 / 转发频率，非法值回退默认并告警）。

## 6.3 关键机制

1. **组转发器（GroupForwarder）**：每组一个转发器封装完整转发语义（期望提取、预测器推进、反馈回发、输出转发），初步地完成解耦（仅限于耦合不强或不耦合的内容），节点退化为订阅 / 发布 / 定时器 / 服务壳。新增转发组只需实例化一个转发器挂到节点组列表，转发逻辑零改动；
2. **模型预测器**：反馈若直接用感知层测量，测量阶梯与延迟会被算法层放大为输出抖动，甚至破坏系统的稳定性。预测器以相对高于采样率的频率控制虚拟对象，用于跟踪真实被控对象，感知层测量到达时对位置与速度分别做校正，首帧测量完成初始化，算法层输出断流时模型冻结，改善控制效果；
3. **断流与订阅保护**：加速度输出超过 500 ms 视为断流，停止转发 `/plant/<组>/cmd`（不下发零值，由仿真层按断流急停）；命令话题无订阅时不转发；转发频率非法值回退默认；
4. **聚合调参服务**：`/control/<节点名>/command` 把调参请求并行路由到各组算法层客户端并统一等待（总超时 2 s）；客户端挂在独立执行器上，不阻塞主转发循环。

模型预测器的原理与数据流见图 6-1。

```mermaid
flowchart LR
    PLT[仿真层 Gazebo] -->|关节状态| PER[感知层测量 p, v]
    PER --> PRED[模型预测器<br/>100 Hz 虚拟对象积分<br/>互补校正 αp / αv]
    PRED -->|/ctrl/组/feedback 平滑模型状态| ALG[算法层]
    ALG -->|加速度| CTL[控制层]
    CTL -->|/plant/组/cmd| PLT
```

*图 6-1 模型预测器原理*

## 6.4 验证

1. 冒烟测试（脱离仿真）：注入假期望与假反馈，断言输出维度与数值有限性、参数话题与头文件默认值一致、输入转发正确、健康话题状态、聚合调参生效；
2. 闭环验证：随 `bringup_control` 完整闭环，健康话题恢复 OK、加速度转发至仿真层、反馈经模型预测器回发算法层；全流程测试以"5 组命令话题在发"作为本层证据。
3. 控制层 HMI 可查看本层状态并下发指令。

## 6.5 限制与问题

1. 反馈是模型状态而非真实位置，模型与真实存在偏差，仅靠低频测量校正；长期无测量（超过 2 s）模型会偏离真实，节点告警；
2. 断流期间反馈照常发布，但为冻结的模型状态；
3. 控制步长固定频率，与真实经过时间存在一定差别，因此在需要真实时间的地方会测量真实时间作为函数参数传递；
4. 算法层未启动时聚合调参可能超时失败，调参应避开启动窗口。

## 6.6 后续规划建议

1. 将校正系数改为运行期可调，便于现场整定；
2. 评估按组分错峰转发，降低瞬时负载，提升系统实时性；
3. 提供对算法层等下层模块的控制能力，强化其作为下位机核心的作用。
4. 补充更多可以发送给上层的信息，供上层决策使用。
5. 目前，弱耦合的内容直接解耦，没有处理算法。后续可以在这部分补充完善。

# 7 ylr1d_translate — 转译层

## 7.1 功能定位

`ylr1d_translate` 位于规划层与控制层之间，接收上层封装好的动作指令，解算为控制层可消费的关节期望值并发布。本层主要是为了与厂家 SDK 的接口能力对齐（接口选取依据见附录 A 的 SDK 测试结论：机械臂仅关节空间控制、底盘仅三种运动模式、夹爪用布尔量控制；附录 B 为真机侧的落地实现）。本层是上位机与下位机在控制链路上的关键接口。

## 7.2 接口

**输入**：

1. `/chassis_move`（动作）：底盘运动指令，字段为 mode、direction、speed、duration；
2. `/arm_move`（动作）：机械臂关节位置指令，字段为 part、positions；
3. `/gripper_move`（动作）：夹爪开合指令，字段为 part、open；
4. `/perception/sensors/joint_states`：感知层清洗后关节状态，用于转向与到位判定、断流检测。

**输出**：

1. `/desired_joint_states`（`sensor_msgs/JointState`）：活跃目标对应关节的期望值，只发布活跃目标涉及的关节，无活跃目标时不发布；
2. 三个动作的反馈与结果：phase（steering / moving）或 progress（已到位关节占比）、success、message。

三个目标独立推进、互不阻塞；新目标到达时中止上一目标，取消时中止当前目标。

## 7.3 解算机制

1. **底盘两阶段运动**：转向与移动共用同一批轮子，边转边动会使运动方向在转向过程中持续变化，因此采用"先转向、后移动"的两阶段：先以轮速钳零转向到目标角度（0.03 rad 容差内），到位后开始移动并计时。三种模式解算如下：

   1. 平移（mode 0）：四轮转向角等于运动方向角，轮速等于车体线速度除以换算比；
   2. 原地旋转（mode 1）：四轮固定转向角，轮速再除以角速度换算比（换算比为负值，使正速度对应逆时针）；
   3. 停车（mode 2）：四轮固定停车转向角，轮速为零，转向到位即成功。

   速度与转向换算常量（线速度换算比、角速度换算比、旋转与停车转向角表）统一在底盘运动学配置头中，与感知层轮式里程计共用；

2. **机械臂解算**：part 取 0（躯干 4 关节）、1（左臂 7 关节）、2（右臂 7 关节），positions 数量与部位关节数不一致时直接中止；按到位容差 0.02 rad 统计已到位关节占比作为进度，全部到位即成功；

3. **夹爪解算**：part 取 0（左夹爪）、1（右夹爪），布尔开合映射为两夹指目标位置（左夹爪 -0.014 m 开 / 0 关，右夹爪 0 / +0.014 m 关），到位容差 0.002 m（夹指行程仅 0.014 m）。

底盘"先转向后移动"两阶段与三种模式的转换关系见图 7-1。

```mermaid
stateDiagram-v2
    [*] --> S1: 新底盘目标
    state "转向（先转向后移动第一阶段）" as S1
    state "移动（计时执行）" as S2
    state "转向超时中止" as S3
    S1 --> S2: 转向到位（0.03 rad 容差）
    S1 --> [*]: 停车模式转向到位即成功
    S2 --> S1: 新目标 / 方向变化
    S1 --> S1: 新目标到达，重新转向
    S2 --> [*]: 时长到达 / 目标完成
    S1 --> S3: 30 s 硬超时
    S3 --> [*]: 中止目标
    S2 --> [*]: 取消
```

*图 7-1 转译层底盘动作状态机*

## 7.4 关键机制

1. **到位判定与超时**：到位判定依赖感知层反馈末帧；转向阶段设 30 s 硬超时，防止反馈缺失时目标无法结束；臂与夹爪到位前检查数据流新鲜度，超过 2 s 判定断流并快速中止（断流检测仅对"曾经收到后断流"生效，从未收到时不断流、开环发布期望）；
2. **期望合并发布**：以 20 Hz 定时把三个活跃目标的期望合并为单一关节状态消息发布；
3. **双轨形态**：默认经跨包 composition 与算法层、控制层同容器运行，另保留独立节点形态用于调试，两种形态的节点名保持一致。

## 7.5 验证

1. 转译链路集成测试（需仿真）：断言三个动作服务在线，发送夹爪目标后 `/desired_joint_states` 收到对应期望（动作到期望值通路）；
2. 闭环验证：随 `bringup_translate` 完整闭环，全流程测试以"三个动作服务在线、底盘目标后期望发布"作为本层证据。
3. 转译层 HMI 可查看本层状态并下发指令。

## 7.6 限制与问题

1. 下层未启动时目标停留在转向阶段或开环发布期望，进度恒为零，动作无法成功；
2. 取消底盘目标只中止并停止发布，控制层保持最后轮速，停车须显式发送停车模式；
3. 控制 HMI 与转译 HMI 同时使用会竞争同一批关节，不应混用（各走对应 bringup 链路）；
4. 底盘到位判定依赖感知层数据，感知层启动异常时转向阶段会占用整个超时窗口。

## 7.7 后续规划建议

1. 兼容更多、更复杂的SDK指令，提升底盘动作连续性；
2. 按目标距离动态调整到位超时，替代固定超时；
3. 评估把断流检测扩展为对感知层数据源的整体健康判断。
4. 增加对控制层的控制功能，便于上位机直接控制下位机。

# 8 ylr1d_perception — 感知层

## 8.1 功能定位

`ylr1d_perception` 是唯一的、统一的传感器数据入口，具体细分为接收层、高级感知层、分发层三层（架构见图 8-1）：
- 接收层接收 27 路传感器及关节状态，进行预处理，提供可靠稳定的信息；
- 高级感知层基于接收层的信息调用高级感知算法，实现较复杂的感知功能（目前仅包括轮式里程计、关节速度 / 加速度估计与定位）；
- 分发层统一对外发布 `/perception/*`，按消费方可分为两大类：\
   1. 本地消费：主要消费方为控制层，频率高、时效性要求高，消费内容以基础信息为主；
   2. 上位机消费：主要消费方为上位机相关节点，频率相对较低、算法较复杂，消费内容以高级感知信息为主。

高级感知层部署在上位机还是下位机，取决于下位机算力是否足以支撑相关算法。

该包的同步框架基于 message_filters（LatestTimeSync）实现。

```mermaid
flowchart TD
    GZ[Gazebo / 仿真层] --> RCV[接收层<br/>五类传感器接收<br/>关节清洗 NaN 置零]
    RCV --> ADV[高级感知层<br/>速度 / 加速度估计<br/>轮式里程计 / 定位]
    ADV --> DSP[分发层<br/>核查 / 同步 / 统一发布]
    DSP --> O1["/perception/joint_state<br/>控制层反馈源"]
    DSP --> O2["/perception/sensors/*<br/>27 路恒等转发"]
    DSP --> O3["/perception/odometry 等<br/>对外出口"]
```

*图 8-1 感知层三段架构*

## 8.2 接收层

1. **五类接收节点**（图像、相机参数、点云、激光、IMU）：以统一模板实现，一种节点处理一类消息。订阅回调只写入槽位，节点内部以多线程方式按 200 Hz 恒等转发至内部话题 `/perception_self/sensors/*`，并修正消息头中的坐标系字段（仿真插件输出的 frame_id 均为主链父帧 `Link_Base`，须改写为真实传感器坐标系）；每 500 ms 汇总断流状态发布健康片段。转发保持恒等、不做滤波降噪，语义无歧义；
2. **关节状态清洗**：`joint_state_receiver` 对 `/joint_states` 做等长校验并把 NaN 置零（仿真暂停阶段移动关节位置初始化为 NaN），以 100 Hz 全帧透传，输出内部话题 `/perception_self/sensors/joint_states`，作为仿真层、转译层与分发层的数据源；
3. **健康聚合**：`health_aggregator` 聚合各接收节点的健康片段，对外发布 `/perception/health`（片段缺失标记为节点不可用）。

## 8.3 高级感知层

1. **关节速度 / 加速度估计**：`joint_state_estimator` 使用二阶非线性跟踪微分器级联（第一级由位置估计速度，第二级由速度估计加速度），对机械臂三组（躯干、左右臂）估计速度与加速度，底盘两组透传；首帧复位防止瞬态，跟踪因子运行期可调；
2. **轮式里程计**：`wheel_odometry` 单路订阅内部关节状态（转向角与轮速在同一消息中，天然时间对齐），按超定最小二乘求解车体速度 (Vx, Vy, ω)，并加入小量正则项防止矩阵奇异（纯平移时侧向速度不可观测，奇异会导致除零）。轮径与轮位取自底盘运动学配置头，输出坐标系为机器人基座系；
3. **定位**：目前导航链路依赖的定位由 `localization.launch.py` 提供：地图服务器加载静态地图，amcl 提供粒子滤波定位（全向运动模型、初始位姿为地图原点、忽略近距激光点、时间容差与更新阈值按运行环境调优），EKF 融合轮式里程计线速度与 IMU 角速度，输出 `/odom` 与 odom→基座系 TF（时间补偿按运行环境调优）。定位参数修改后必须重新构建安装本包。
4. **扩展机制（高级感知扩展约定）**：新增高级感知节点（视觉里程计、激光定位、目标检测、底盘组速度 / 加速度估计等）统一放在 `advanced/` 目录，输出内部话题、对外出口经分发层注册，保持单一出口（见 8.4 节）；多路传感器时间同步框架（`SyncUnit`）已内置、当前为单路订阅未实例化，需要多路汇聚时实例化即可（见 8.8 节）。若需要launch文件，可以将对应的文件放在launch文件夹中

## 8.4 分发层

分发层基于"分发骨架 + 同步单元"实现，每个同步单元独立线程轮询：

1. **控制反馈核查**：`control_feedback_guard` 对五组估计数据做维度、数值、范围、新鲜度核查（位置钳制物理限位，估计组速度与加速度限幅，底盘组透传不钳），通过后发布 `/perception/joint_state`；
2. **传感器分发**：`sensor_dispatch` 对外发布 27 路传感器、全量关节、五组分话题（纯原始关节数据，不含估计值、不钳制）与里程计出口。

内部话题（`/perception_self/*`）与对外话题（`/perception/*`）前缀隔离，对外数据全部经分发层核查后发布，保证单一出口（健康话题除外，由聚合节点直接发布）。

## 8.5 接口

**输入**：

1. `/joint_states`：仿真层 30 关节全量状态；
2. Gazebo 原始传感器话题 27 路：图像 / 相机参数 / 点云 / 激光 / IMU。

**输出**：

1. `/perception/joint_state`：核查后的五组位置 / 速度 / 加速度（控制层反馈源）；
2. `/perception/sensors/joint_states`：清洗后的全量干净关节（供仿真层、转译层、rsp 消费）；
3. `/perception/sensors/*`：27 路传感器恒等转发（供 HMI / rviz 消费）；
4. `/perception/hmi/joints/<组>`：五组纯原始关节；
5. `/perception/odometry`：轮式里程计对外出口；
6. `/perception/health`：健康状态（500 ms）。

## 8.6 验证

1. 冒烟测试（假数据，无需仿真）：注入含 NaN 的关节数据断言清洗、注入关节位置序列断言速度估计、注入超限数据断言核查钳制、断言传感器恒等转发、断言健康格式、纯平移与纯旋转两场景断言轮式里程计解算；
2. 闭环验证：随 bringup 在 0 秒并入，作为控制层反馈源；对外传感器数据供 HMI 展示。

## 8.7 限制与问题

1. 数据源依赖仿真层，无仿真时接收与清洗层空转，独立启动须先启动仿真层；
2. 传感器话题使用深度为 1 的队列，与对端发布 / 订阅 QoS 需保持一致；
3. 轮式里程计受轮子情况影响，存在侧向速度残差与航向漂移，加之传感器频率偏低，定位精度受限（见 14.2 节）。

## 8.8 后续规划建议

1. 补充相关高级感知（视觉里程计、目标检测），接入视觉抓取；
2. 增加更多鲁棒性设计，增强系统可靠性。


# 9 ylr1d_plan — 规划层

规划层拆分为两个平级且彼此独立的包：`ylr1d_plan_nav`（导航）与 `ylr1d_plan_moveit`（机械臂规划）。两者采用同一框架：上层动作入口 → 规划器（nav2 / MoveIt2）→ 桥接节点 → 转译层动作执行，所需信息由感知层提供。

## 9.1 导航（ylr1d_plan_nav）

### 9.1.1 功能定位

提供最小化 nav2 导航栈（全局规划、局部规划、行为树导航、恢复行为及生命周期管理），并由 `cmd_vel_bridge` 把 nav2 输出的连续速度指令分段为转译层底盘动作下发执行，链路数据流见图 9-1。

### 9.1.2 导航参数

nav2 参数集中于单一配置文件，关键项如下：

1. 代价地图采用双层命名空间结构（nav2 Humble 要求，单层会被静默忽略导致机器人坐标系与插件回落默认值），机器人坐标系设为基座系，局部代价地图必须为滚动窗口（固定窗口在定位漂移后机器人会出界）；
2. 全局规划器为 NavFn，局部规划器为 DWB 全向规划，速度上限（线速度 0.5 m/s、角速度 1.0 rad/s），到位检查容差 0.25 m；
3. 行为树导航的行为树插件库取官方发行包全表（保证本机全部存在），行为树节点等待服务响应的超时设为 20 s（默认值 20 ms 在重负载下必然超时导致导航失败），行为树循环周期 100 ms；
4. 恢复行为包含旋转、后退、等待三种。

### 9.1.3 速度指令分段与桥接

转译层底盘动作是离散目标（先转向后移动），而 nav2 输出连续速度，若每帧都作为新目标下发，会反复触发转译层"新目标抢占旧目标"、转向被中断，机器人无法到位。因此桥接节点按以下机制分段：

1. **转换状态机**（与 ROS2 通信解耦的纯 C++ 模块）：连续速度近似为零时输出暂停（保持姿态、目标不结束、可随时恢复）；纯旋转输出旋转目标；有平移分量时输出平移目标（方向取速度合成角）；停车只由显式停止请求触发，不对零指令自动产生。状态机是预留的扩展点：扩展分段策略（运动分解、多段路径、连续运动优化等等）只改转换器实现与桥接节点构造处的 `make_unique<XxxConverter>()`，通信骨架零改动（见 9.4 节）；
2. **防抢占去重**：在途目标不叠加、相同目标不重发、方向或速度显著变化时才重发，模式切换带滞回（约 1 s），从机制上避免反复打断转向；
3. **桥接节点**只做通信：订阅速度话题、维护目标生命周期（在途看门狗、断流自动停止）。发送时角速度取反（对齐转译层旋转方向约定），平移与旋转速度按系数放大（导航规划输出速度偏小）。

```mermaid
flowchart 
    SRC[决策层 / HMI] -->|NavigateToPose| NAV[nav2 导航栈<br/>全局规划 / 局部规划 / 行为树]
    NAV -->|连续速度 /cmd_vel| BRG[cmd_vel_bridge<br/>三态分段转换状态机]
    BRG -->|/chassis_move 动作| 下层 -->|传感器数据| PER[感知层定位<br/>amcl + EKF]
    PER -->|map 系位姿| NAV
```

*图 9-1 导航链路数据流*

### 9.1.4 验证

1. 静态校验（无需仿真）：断言导航参数结构（双层代价地图、机器人坐标系、超时与滚动窗口）、行为树插件库存在性、launch 节点装配与显示配置；
2. 状态机自测：`--logic-test` 18 条断言覆盖分段、去重、暂停、停止语义；
3. 闭环验证：随 `bringup_plan_nav` 实跑导航目标。
4. 规划导航 HMI 可观察并控制导航过程。

## 9.2 机械臂规划（ylr1d_plan_moveit）

### 9.2.1 功能定位

承载"决策层动作 → MoveIt 规划 → 转译层执行"链路：move_group 提供逆运动学与避障规划，`moveit_goal_server` 对决策层动作做组合编排（先臂后爪），`moveit_bridge` 把内部原子目标转为规划请求、把轨迹离散为路点序列逐段下发转译层执行；同时，bridge 也是与下位机接口的核心组成部分（详见附录B的实例）。规划只负责"算"与编排，关节运动执行走转译层（不绕开控制闭环）；夹爪由原子接口独立控制，不进入规划组。位姿目标统一使用机器人基座系，链路数据流见图 9-2。

### 9.2.2 配置生成

MoveIt 配置由脚本从 xacro 现场生成，保证与当前模型严格一致，产物包括：语义描述（躯干、左右臂三个规划组、末端执行器、相邻碰撞矩阵）、运动学求解器配置（KDL）、规划器配置（OMPL）、关节限位，以及两个占位配置（MoveIt 构建器期望存在，实际执行不走 FollowJointTrajectory 而走转译层动作）。

### 9.2.3 接口与编排

**输入**：`/plan/moveit/moveit_move`（动作，对外入口），字段为 part（0 躯干 / 1 左臂 / 2 右臂）、位姿或关节位置、夹爪模式（-1 不动 / 0 张开 / 1 闭合）。

**内部**：goal_server 与 bridge 之间以四类原子话题通信（臂目标、夹爪目标、控制指令、执行结果），统一使用 `/plan/moveit/` 前缀。

**编排与执行**：

1. goal_server 把组合目标拆为"先臂后爪"两段下发，跟踪两段完成，夹爪失败整单中止，忙碌时拒绝新目标，取消走内部控制通道（单段超时 90 s）；
2. bridge 以全异步回调链调用 move_group 的逆运动学与规划服务（回调内只发起下一步请求、不阻塞），规划看门狗 20 s，结果语义为"执行完成"而非"规划成功"；轨迹经等距采样离散为路点序列（默认步距 0.15 rad），逐路点下发转译层动作，任一路点失败中止剩余链。同时，bridge 也是与下位机接口的核心组成部分（详见附录B的实例）；
3. **躯干视觉瞄准**：躯干的核心目标并非让躯干末端到达特定位置，而是使其末端的摄像头对准目标位置。相机位于躯干组之外的固定关节下游，直接对相机位姿求逆运动学不可行（求解器局部收敛跨不过大角度差），因此对躯干单独实现纯 C++ 解算器：以方位角引导的多起点非线性最小二乘，求相机光轴穿过目标点的 4 关节解，并给出可达性判定（光轴误差与距离约束）；解算器内置正运动学对拍与数学自检模式。

```mermaid
flowchart 
    SRC[决策层 / HMI] -->|/plan/moveit/moveit_move 动作| GS[moveit_goal_server<br/>组合编排：先臂后爪]
    GS -->|臂 / 夹爪原子目标| BRG[moveit_bridge<br/>全异步规划回调<br/>轨迹等距离散]
    BRG <-->|IK / 规划服务| MG[move_group<br/>OMPL 避障规划]
    BRG -->|逐路点 /arm_move| 下层

```

*图 9-2 机械臂规划链路数据流*

### 9.2.4 验证

1. 静态校验（无需仿真）：安装副本语义描述与 URDF 一致性、三个规划组、末端执行器、夹爪不进组、运动学与规划器配置存在性；
2. 端到端 mock（模拟下层）：动作客户端 7 个用例（正运动学对拍、组合先臂后爪、只臂、只夹爪、忙碌拒绝、取消、躯干瞄准成功率）；
3. 闭环验证：随 `bringup_plan_moveit` 实跑机械臂目标。
4. 规划机械臂 HMI 可查看并控制规划过程。

## 9.3 限制与问题

1. 端到端导航存在约 0.4 m 的到位偏差：轮子打滑导致定位漂移，导航基于里程计判定到位而定位基于地图系（见 14.2 节）；
2. move_group 服务就绪慢（约 100 s），固定时延分层下首个目标可能早于服务就绪，需重试或等待；
3. 启动前必须清理残留进程与共享内存锁（占用 Fast DDS 端口会使 move_group 初始化失败），且单独启动的进程须与 bringup 树处于同一 DDS 组；
4. 躯干转向后，臂的固定基座系位姿可能不可达（属几何事实），臂目标建议在躯干目标位形下经 TF 查询可达位姿；
5. 高负载下 move_group 偶发卡死，重启或降载自愈，mock 测试规避该环境敏感问题；
6. 导航结束或上层停止后桥接节点不自动触发停车，依赖速度输出为零转为暂停，速度断流超过 1 s 才自动停止。

## 9.4 后续规划建议

1. 目前功能还不稳定，主要表现在多次规划任务时，规划、控制效果显著降低，重复性差。
2. 利用nav2、moveit2，进一步完善相关功能。
3. 完善桥接部分状态机。
4. 增加完善高级感知功能，实现更加复杂的规划任务，例如接入相机链路实现视觉伺服；
5. 评估更换运动学求解器与规划器（TRAC-IK、其他规划器）提升规划质量。
6. 增加更多的、更加灵活的控制方法，例如机械臂臂形角控制等内容。
7. 增加在逆运动学解算失败的处理，目前仅仅抛出问题，没有解决。
8. 增强系统的鲁棒性。

# 10 ylr1d_decision — 决策层

## 10.1 功能定位

`ylr1d_decision` 位于规划层之上，承担任务编排：把任务拆解为规划层动作序列并驱动执行。内部按"顶层接口、中层决策、下层对接"分三层，三个节点经 ROS2 动作通信，launch 用 composition 封装进同一容器；任务以行为树（BehaviorTree.CPP v3）XML 表达。

决策层是上位机的控制核心与数据中枢，消费绝大多数高级感知信息，并对相关内容进行管理。

**边界约定**：决策层只调用规划层两个动作（导航与机械臂组合动作），不触碰转译层动作、不下发关节指令；只读取感知层状态（定位、里程计、TF、关节状态、健康话题）用于条件判断与坐标融合。关节解算与轨迹规划由规划层完成。

## 10.2 三层结构与接口

三层节点与动作接口的装配关系见图 10-1。

| 层 | 节点 | 职责 | 接口 |
|----|----|------|-----|
| 顶层 | `mission_server` | 接收任务、解析参数、转发中层、跟踪任务生命周期 | `/decision/mission`（动作，对外入口） |
| 中层 | `decision` | 通用行为树引擎：加载任务 XML 并执行；叶子节点读取 TF 融合出绝对目标 | `/decision/run` （动作，层间）；`/decision/status`、`/decision/health` （对外） |
| 下层 | `plan_client` | 包装规划层两个动作客户端，下发绝对目标、接收结果、传播取消 | `/decision/plan/navigate`、`/decision/plan/moveit`（动作，层间） |

中层为通用引擎：任务即行为树 XML 数据，更换任务只需更换 XML（节点类型已注册即可），引擎节点零改动；只有出现全新的叶子行为（新能力）才需注册新节点类型。新增任务属预留接口完善（见 1.5.2 节），与缺陷改进区分。

```mermaid
flowchart TD
    subgraph TOP[顶层]
        MS[mission_server<br/>/decision/mission 动作]
    end
    subgraph MID[中层]
        DC[decision 行为树引擎<br/>/decision/run 动作]
    end
    subgraph BOT[下层]
        PC[plan_client<br/>规划层动作客户端]
    end
    MS -->|任务解析 / 生命周期| DC
    DC -->|执行任务 XML| PC
    PC -->|/decision/plan/navigate| NAV[规划层导航]
    PC -->|/decision/plan/moveit| MOV[规划层机械臂]
    DC -.->|读取 TF / 感知层状态| TF[TF / 高级感知信息]
```

*图 10-1 决策层三层结构*

## 10.3 对外接口

1. `/decision/mission`（动作）：任务名 + 参数数组。参数按任务约定：底座为 [dx, dy, dθ]，机械臂为 [part, dx, dy, dz, droll, dpitch, dyaw]，躯干瞄准为 [x, y, z]（地图系瞄准点）；反馈含阶段与进度，结果含结果码（成功 / 失败 / 取消 / 超时）与消息；
2. `/decision/status`（字符串）：任务事件（已接受、执行中、结果）；
3. `/decision/health`（字符串，500 ms）：OK / BUSY / 规划客户端不可用。

## 10.4 任务示例

当前提供三个演示任务：

1. **底座相对移动**：读取 TF 得到当前位姿，加增量（偏航角归一化）得到地图系绝对目标，经导航动作下发 nav2；任务下发与执行的决策层界面见图 10-2。

![图 10-2 决策层任务演示](images/decision-move.png)

*图 10-2 决策层任务演示*
2. **机械臂末端相对移动**：读取 TF 得到当前末端位姿，加平移增量，姿态增量经四元数合成（欧拉角转四元数后与当前姿态合成），得到基座系绝对目标，经机械臂组合动作下发 MoveIt；
3. **躯干瞄准**：直接输入地图系瞄准点，转为基座系后经机械臂组合动作下发（规划层解算器求 4 关节解，使相机光轴穿过瞄准点）。

## 10.5 鲁棒性

1. 任务超时：导航 300 s、机械臂 90 s，行为树叶子与规划客户端双侧看门狗；
2. 失败回退：行为树支持重试与回退节点（当前演示任务为纯顺序执行）；
3. 取消传播：任务取消逐层传播，行为树中止并取消在途规划层动作；
4. 健康门控与状态上报：健康话题反映规划客户端可用性与任务忙闲，状态话题发布任务事件。

## 10.6 验证

1. 端到端 mock（模拟规划层）：底座与机械臂相对移动（TF 融合、增量合成、姿态合成）、取消传播，全部通过；
2. 真实仿真集成（默认跳过，约 8 至 12 分钟）：`bringup_decision` 全流程，底座导航与机械臂执行端到端通过；
3. HMI 冒烟：决策面板无显示环境启动存活。

## 10.7 限制与问题

1. 底座导航依赖转译层"先转向后移动"分段语义，重负载下小目标执行慢（导航超时已放宽到 300 s 兜底）；
2. 运行环境下偶发出现定位节点被生命周期管理误杀（定位失效告警），重启 bringup 自愈；
3. 演示任务为纯顺序执行，尚无任务失败后的自动回退策略。

## 10.8 后续规划建议

1. 扩展任务库（抓取序列、多段组合任务），编排能力沉淀为可复用行为树节点；实现更复杂任务的管理编排；
2. 接入任务失败回退与重试策略；
3. 增加传感器相关功能，便于决策层作出更加准确可靠的决策；
4. 顶层任务获取的时候，可以使用更加高级的方法，分析需要执行的任务，进行更加智能的决策；
5. 决策层可以增加对所有层相关参数的控制功能，提升整体控制效果。

# 11 ylr1d_hmi — 人机界面

## 11.1 功能定位

`ylr1d_hmi` 提供基于 Qt5 的人机交互界面（八个面板，覆盖从底层关节到规划层的观测与指令下发）与四个 rviz2 面板插件。HMI 只做观测与指令下发，不参与控制闭环。职责划分约定：**rviz2 面板负责系统稳定性监视（节点是否正常运行），HMI 面板负责控制过程（指令、时间、执行过程）**。

## 11.2 Qt5 面板

八个面板对应九个可执行程序（传感器面板含感知层与直连两个版本），职责如下：

- **控制面板**：四个标签页对应底盘、躯干、左臂、右臂；观测关节状态，滑杆下发期望值，以 5 Hz 合并发送（拖动仅置脏，由发送定时器统一发送，解耦界面交互频率与发布频率），发送频率可调，支持自动发送与手动发送。该面板是底层关节的直接控制入口：每页显示对应分组关节的当前状态（位置 / 速度），拖拽滑杆仅标记期望值，由发送定时器按设定频率统一合并下发，避免拖动过程高频刷包；自动发送模式用于持续跟踪目标位形，手动模式用于单次确认下发。底盘、躯干、机械臂三个标签页见图 11-1 至图 11-3。

![图 11-1 控制面板——底盘标签页](images/control-vehicle.png)

*图 11-1 控制面板——底盘标签页*

![图 11-2 控制面板——躯干标签页](images/control-torso.png)

*图 11-2 控制面板——躯干标签页*

![图 11-3 控制面板——机械臂标签页](images/control-arm.png)

*图 11-3 控制面板——机械臂标签页*
- **转译面板**：三个动作客户端（底盘、机械臂、夹爪），带底盘指令可视化（平移箭头、旋转弧、停车方块）与事件日志。该面板是动作层（Action）的控制入口：底盘页按转译层三模式组织（平移 / 原地旋转 / 停车，可设方向、速度与时长，指令可视化同步显示运动意图）；机械臂页按 part 选择部位（0 躯干 / 1 左臂 / 2 右臂）并下发关节位置；夹爪页以布尔开关控制开合；面板底部事件日志记录每个动作的结果与错误信息。底盘、机械臂（含躯干）、夹爪三个页面见图 11-4 至图 11-6。

![图 11-4 转译面板——底盘动作页](images/translate-vehicle.png)

*图 11-4 转译面板——底盘动作页*

![图 11-5 转译面板——机械臂与躯干动作页](images/translate-arm-torso.png)

*图 11-5 转译面板——机械臂与躯干动作页*

![图 11-6 转译面板——夹爪动作页](images/translate-finger.png)

*图 11-6 转译面板——夹爪动作页*
- **传感器面板**（两个版本）：感知层版本只读观测 27 路传感器与关节分组；直连版本订阅仿真原始话题。该面板按传感器类型分页展示实时数据：相机页显示彩色图像（图 11-7）、深度相机页显示深度图（图 11-8）、IMU 页显示三轴加速度 / 角速度曲线（图 11-9）、雷达页显示激光扫描数据（图 11-10）、超声波页显示四角测距（图 11-11）；关节分组视图汇总 30 关节状态。感知层版本消费 `/perception/sensors/*`（清洗核查后数据），直连版本直接订阅 Gazebo 原始话题，用于对比感知层处理前后差异。

![图 11-7 传感器面板——相机](images/sensor-camera.png)

*图 11-7 传感器面板——相机*

![图 11-8 传感器面板——深度相机](images/sensor-deep-camera.png)

*图 11-8 传感器面板——深度相机*

![图 11-9 传感器面板——IMU](images/sensor-imu.png)

*图 11-9 传感器面板——IMU*

![图 11-10 传感器面板——雷达](images/sensor-radar.png)

*图 11-10 传感器面板——雷达*

![图 11-11 传感器面板——超声波（ray）](images/sensor-ray.png)

*图 11-11 传感器面板——超声波（ray）*
- **算法层面板**：只读监视五组共 25 个话题（期望、反馈、输出、参数、状态）的存活、频率与维度，展示最近一帧。该面板用于控制链路运行体检：对 steering / wheels / torso / left_arm / right_arm 五组分别展示 `/ctrl/<组>/{input,feedback,output,param,state}` 五个话题的发布频率、数据维度与最近一帧数值，任一话题断流或维度异常可立即发现；面板本身不提供指令下发（调参走 `/ctrl/<组>/command` 服务）。界面见图 11-12。

![图 11-12 算法层面板](images/algorithm.png)

*图 11-12 算法层面板*
- **规划导航面板**：导航动作客户端，显示目标、位姿、指令记录与底盘运动可视化。该面板封装 `NavigateToPose` 动作：设置目标位姿（x / y / yaw）下发导航栈，显示当前位姿与目标状态（进行中 / 成功 / 失败 / 取消），指令记录区按时间列出历次导航指令，底盘运动可视化把 nav2 的连续 `cmd_vel` 映射为平移 / 旋转 / 暂停三态显示，与控制过程对应。界面见图 11-13。

![图 11-13 规划导航面板](images/plan_nav.png)

*图 11-13 规划导航面板*
- **规划机械臂面板**：机械臂组合动作客户端（部位、位姿或瞄准点、夹爪模式、仅夹爪），支持取消与执行状态显示。该面板封装 `/plan/moveit/moveit_move` 组合动作：按部位（0 躯干 / 1 左臂 / 2 右臂）选择目标形式——位姿目标（基座系位置与姿态）、关节位置目标或躯干瞄准点（相机光轴对准目标点），并设置夹爪模式（-1 不动 / 0 张开 / 1 闭合）；面板显示组合编排进度（先臂后爪两段）、执行状态与结果，支持取消在途目标。"仅夹爪"模式只下发夹爪动作，便于单独调试。界面见图 11-14。

![图 11-14 规划机械臂面板](images/plan_moveit.png)

*图 11-14 规划机械臂面板*
- **决策面板**：向决策层发送任务；

- **监视面板**：独立监视窗口（节点名 `ylr1d_hmi_monitor`），与 rviz2 监视面板共用同一监视组件，显示各层健康话题与节点状态。该面板是系统稳定性监视入口，按"静态结构 + 动态探查"设计（见 11.4 节）：总览视图按分层列出决策 / 规划 / 转译 / 控制 / 算法 / 仿真 / 感知各层核心节点及其健康状态（OK / 异常）与关键话题新鲜度（图 11-15、图 11-16）；关节视图汇总 30 关节状态与限位（图 11-17）；日志视图展示监视组件的事件记录（图 11-18）；传感器视图显示 27 路传感器数据与感知层健康（图 11-19）；控制节点视图按层列出全部节点（图 11-20）与指定节点详情（图 11-21），支持按层重启故障节点（经容器看门狗自动拉起）。

![图 11-15 监视面板——总览](images/monitor-overview.png)

*图 11-15 监视面板——总览*

![图 11-16 监视面板——总览（扩展视图）](images/monitor-overview2.png)

*图 11-16 监视面板——总览（扩展视图）*

![图 11-17 监视面板——关节状态](images/monitor-joints.png)

*图 11-17 监视面板——关节状态*

![图 11-18 监视面板——日志](images/monitor-log.png)

*图 11-18 监视面板——日志*

![图 11-19 监视面板——传感器健康](images/monitor-sensors.png)

*图 11-19 监视面板——传感器健康*

![图 11-20 监视面板——控制相关节点（全部）](images/monitor-control-all-nodes.png)

*图 11-20 监视面板——控制相关节点（全部）*

![图 11-21 监视面板——控制相关节点（指定）](images/monitor-control-certain-nodes.png)

*图 11-21 监视面板——控制相关节点（指定）*

## 11.3 rviz2 面板插件

rviz2 插件库声明四个面板类：

- **监视面板**：显示下位机相关的系统健康话题，订阅话题与 rviz 显示项不重叠，使用全局上下文；

- **导航状态面板**：显示导航栈节点在线状态、指令转换桥接节点状态与定位节点状态（定位、里程计新鲜度）；

![图 11-22 rviz2 导航规划场景](images/plan-nav-rviz2.png)
*图 11-22 rviz2 导航规划场景*

- **机械臂状态面板**：显示机械臂规划相关节点与接口状态；rviz2 场景中机械臂规划的显示效果（机器人模型、规划组与目标）见图 11-23。

![图 11-23 rviz2 机械臂规划场景](images/plan-moveit-rviz2.png)

*图 11-23 rviz2 机械臂规划场景*

- **决策层状态面板**：显示决策层相关节点与接口状态；决策任务在 rviz2 中的显示见图 11-24。

![图 11-24 rviz2 决策层](images/decision-rviz.png)

*图 11-24 rviz2 决策层场景*


导航状态、机械臂状态与决策层状态三个面板的订阅话题与 rviz 显示项存在重叠，使用独立 ROS 上下文（若用全局上下文订阅与显示重叠的话题，会因类型支持库来源不同导致面板加载失败）。**规律：面板订阅的每个话题都要确认是否与 rviz 显示项重叠，重叠就必须使用独立上下文。**

## 11.4 关键机制

1. **单线程模型**：面板回调运行在界面线程（定时器周期调用消息泵），回调只缓存最新消息，免锁无竞态，是低负载观测型界面的最简单正确模型；
2. **监视面板静态结构 + 动态探查分离**：分层、核心节点、健康话题、重启目标维护在层注册表（唯一半静态结构），节点详情经图接口动态探查（话题速率采样、参数 / 服务 / 动作详情）；按层重启通过精确进程名匹配终止容器进程，由容器看门狗自动拉起；
3. **健康解析统一**：控制、仿真、感知三套发布端的健康字符串由统一解析器解析，监视面板与自动化测试共用同一语义，修改健康语义须同步发布端、解析端与测试断言；
4. **静态配置单一来源**：30 关节定义与传感器话题视图集中在两个配置头，修改限位与话题只改这两处。

## 11.5 验证

1. HMI 冒烟测试：以无显示环境启动各面板可执行程序，断言存活（不崩溃）；
2. 闭环验证：HMI 随 bringup 分层启动，监视面板在仿真运行时显示有效状态。

## 11.6 限制与问题

1. 控制 HMI 与转译 HMI 不应混用（会竞争同一批关节），完整链路各走对应 bringup；
2. 单线程模型在高负载下界面可能卡顿；
3. 软渲染环境下三台相机不能同时全量渲染，界面只渲染选中相机；
4. 监视面板与独立监视窗口共用节点名，同一 DDS 组内并行运行会重名；
5. rviz 布局（面板与显示项）必须直接修改配置文件，界面手动保存会残留旧面板名；
6. 单独启动 HMI 必须与 bringup 树处于同一 DDS 组，否则互相不可见、无法控制（见 2.2 节）。

## 11.7 后续规划建议

1. 增加控制指令与执行记录的本地持久化，便于复盘；
2. 高负载场景下拆分界面线程与消息线程（引入锁 / 队列）；
3. 结合决策层扩展任务型界面（任务下拉、参数表单已具备，进一步支持任务状态可视化）。
# 12 ylr1d_bringup — 一键启动编排

## 12.1 功能定位

`ylr1d_bringup` 是一键启动编排包，聚合各层 launch 为单一启动入口，本身不含节点，只做启动编排与参数透传。五套链路及其内容如下：

1. `bringup_control`：仿真层、感知层、控制层（内嵌算法层）、控制 HMI、传感器面板——控制层 HMI 直调关节；
2. `bringup_translate`：仿真层、感知层、转译层跨包容器、转译 HMI、传感器面板——动作驱动；
3. `bringup_plan_nav`：仿真层、感知层、定位、转译层、nav2 与桥接节点、规划 HMI——导航演示（世界参数传导航测试地图）；
4. `bringup_plan_moveit`：仿真层、感知层、转译层、MoveIt 规划、规划 HMI——机械臂规划演示；
5. `bringup_decision`：仿真层、感知层、定位、转译层、nav2 与 MoveIt 同栈、决策层、决策 HMI、单一决策 rviz——决策层全栈演示。

## 12.2 启动时序

各层就绪存在硬依赖：控制层反馈依赖感知层（感知层须与仿真层同时启动）；仿真层控制器管理就绪时间在运行环境下波动较大（实测 22 至 63 s），上层必须等待。运行环境下，事件驱动（检测就绪后再启动）依赖不稳定的发现机制、并不可靠，因此采用固定时延分层启动（时序见图 12-1）：

1. 0 秒：仿真层（含 Gazebo 与 rviz2）+ 感知层（导航链路另加定位）；
2. 30 秒：控制层与算法层（转译链路为控制、算法、转译三层的跨包容器；导航与机械臂链路为转译层跨包容器）；
3. 40 秒：控制 HMI / 转译 HMI / nav2 与桥接节点 + 规划 rviz / MoveIt 规划栈；
4. 50 秒：规划 HMI（导航、机械臂链路）；决策链路为决策层容器 + 单一决策 rviz；
5. 55 秒：决策 HMI（决策链路）。

```mermaid
flowchart TD
    T0[0 s<br/>仿真层 + 感知层<br/>Gazebo / rviz2 / 感知节点] --> T30[30 s<br/>控制层 + 算法层<br/>转译链路为三层跨包容器]
    T30 --> T40[40 s<br/>控制 / 转译 HMI<br/>nav2 与桥接 / MoveIt 规划栈]
    T40 --> T50[50 s<br/>规划 HMI<br/>决策层容器 + 决策 rviz]
    T50 --> T55[55 s<br/>决策 HMI]
```

*图 12-1 一键启动分层时序*

固定时延只覆盖常见场景：控制器就绪超过 30 秒时，控制层短暂显示等待或下游无订阅状态，就绪后自动恢复，HMI 在 40 秒启动仍可用。修改时序须同步修改五个 launch 与本报告描述。

## 12.3 关键机制

1. **单实例互斥锁**：启动编排在生成阶段以原子方式创建锁文件（含进程号），持锁进程存活时第二个 bringup 直接拒绝启动（五套链路都含仿真层，并行启动会抢占 Gazebo 端口）；正常退出自动释放，异常退出残留锁由清场工具删除，下次启动时按进程号存活检测自动清理陈旧锁并重试；
2. **环境注入**：顶层统一注入 `ROS_LOCALHOST_ONLY=1`（须对全部子进程一致生效，否则单独进程出组互相不可见）；规划与决策链路另注入 `YLR1D_DISABLE_CAMERAS=1` 关闭相机渲染以降低仿真占用（需视觉时去掉）；
3. **参数改名隔离**：被包含的子 launch 中同名参数声明会覆盖父层参数（实测导致规划 rviz 不启动或重复启动），因此仿真层、定位、规划 rviz 的开关参数分别改名隔离，只有规划的 rviz 开关由父层透传。新增子 launch 时沿用该约定，避免使用通用参数名。

## 12.4 验证

本包为纯编排、无运行期接口，验证依赖各层测试与实跑：各层测试见第 13 章；实跑验证为启动后轮询控制器全部 active，且任意两个 bringup 并行启动会被互斥锁拒绝。

## 12.5 限制与问题

1. 五套链路共用同一把互斥锁，不能同时启动；
2. WSL 下整体启动慢（到控制器 active 需 30 至 60 秒），不能抢跑；
3. 依赖顺序固定：任一环节失败需整栈重启；仿真层残留进程由仿真 launch 启动前清理，异常退出后用清场工具清理；
4. 固定时延为经验值，慢场景下存在短暂降级窗口（见 12.2 节）；
5. 修改 launch 后必须重新构建安装本包，否则运行时加载旧副本（见 2.3 节）。

## 12.6 后续规划

1. 评估以事件驱动（检测控制器就绪）替代固定时延，消除慢场景降级窗口；
2. 开放互斥锁粒度，支持不同世界参数下的多实例并行；
3. 补充决策链路的相机开关透传，为视觉抓取做准备。
# 13 ylr1d_test — 测试体系

## 13.1 功能定位

`ylr1d_test` 是系统的功能测试包，统一调度各层测试并落盘结果，失败时给出问题信息与解决建议。约定：新增或改动功能包时同步补充对应测试；需要 ROS2 的测试使用本包自带的独立 launch（复用而非修改 bringup），保证测试与被测编排解耦、失败时可单层定位。

## 13.2 测试分级

测试按依赖与覆盖范围分为三类，另有两类专项：

1. **环境检查**：不依赖仿真，检查工具链、工作空间完整性、残留进程与跨层配置一致性（如关节分组、限位在各层副本是否一致）；
2. **静态与冒烟测试**：不依赖仿真，覆盖各层静态配置校验（模型、参数、配置结构）与脱离仿真的逻辑验证（单元测试、假数据闭环、状态机自测、界面存活）；
3. **仿真集成测试**：依赖真实 Gazebo，覆盖仿真层、转译链路与全流程；
4. **故障注入专项**：注入断流、急停、健康话题死亡、组件崩溃等故障，验证鲁棒性机制；
5. **端到端专项（mock）**：以 mock 下层启动规划与决策栈，验证动作客户端全用例（组合编排、取消、拒绝、瞄准成功率等），规避仿真环境敏感问题。

## 13.3 测试清单

全部 16 项测试如下（括号内为是否需要仿真）：

1. `env`（否）：环境检查，含跨层配置一致性；
2. `desc`（否）：模型资产静态校验（URDF 生成、30 关节、关键限位、传感器插件）；
3. `algorithm`（否）：算法层零 ROS 单元测试（计算输出、参数路由、异常路径）；
4. `control`（否）：控制层冒烟（输出维度与数值、参数话题一致性、健康话题、调参生效）；
5. `perception`（否）：感知层冒烟（清洗、估计、转发、核查、分发出口、健康、轮式里程计）；
6. `plan`（否）：导航静态校验与状态机自测（18 条断言）；
7. `moveit`（否）：机械臂规划静态校验（语义描述、规划组、配置存在性）；
8. `hmi`（否）：各面板无显示环境启动存活；
9. `fault`（否，显式运行）：故障注入（断流停转、模型冻结、急停三态、健康话题死亡、容器重启恢复）；
10. `moveit_e2e`（否，显式运行）：机械臂端到端 mock（动作客户端 7 用例）；
11. `decision_e2e`（否，显式运行）：决策层端到端 mock（任务融合、姿态合成、取消传播）；
12. `plant`（是）：仿真层（控制器 active、关节数据、进程存活）；
13. `translate`（是）：转译链路（动作服务在线、动作到期望值通路）；
14. `sensor`（是，显式运行）：传感器探测（激光话题收到数据）；
15. `full_flow`（是）：全流程端到端（含分层隔离诊断）；
16. `decision`（是，默认跳过）：决策层真实仿真全流程（约 8 至 12 分钟）。

默认集为前 8 项（均不依赖仿真），作为日常回归基线。

## 13.4 使用方法

```bash
bash src/ylr1d_test/scripts/run_tests.sh --tests env       # 环境检查（秒级）
bash src/ylr1d_test/scripts/run_tests.sh                    # 默认集（约 1 分钟）
bash src/ylr1d_test/scripts/run_tests.sh --all              # 全量（含仿真类）
bash src/ylr1d_test/scripts/run_tests.sh --tests <id,...>   # 指定测试
```

1. 入口脚本自动加载工作空间环境；任一测试失败退出码非零，适合接入持续集成；
2. 结果落盘到 `test_results/<时间戳>/`（结构化汇总、文本汇总、逐测试日志），另保留最近一次运行目录；
3. 失败时自动对照已知问题库给出可能原因与解决建议。

## 13.5 分层隔离诊断

全流程测试内置证据链诊断：全流程失败时症状在最顶层而根因可能在任何一层，按证据链从底层向上核查并自动补跑对应层测试，把"整条链哪里断"收敛为"哪一层断"。证据链顺序：

1. 6 个控制器全部 active → 定位到仿真层；
2. `/joint_states` 有数据 → 定位到仿真层；
3. 三个动作服务在线 → 定位到转译层；
4. 底盘目标后期望话题有期望 → 定位到转译层；
5. 5 组命令话题在发 → 定位到控制层；
6. 运动断言（转向 / 轮速）——前五项全过仍失败则问题在仿真、时序或环境层。

## 13.6 限制与问题

1. 仿真类测试耗时较长且对运行环境敏感（偶发发现失败、加载慢），默认集不含仿真类，全量回归需人工触发；
2. 修改测试模块后必须重新构建并安装测试包，否则运行旧副本（新增断言不执行、测试仍通过，比失败更隐蔽）；
3. mock 测试与真实仿真的覆盖存在差异，端到端结论以仿真集成与实跑为准。

## 13.7 后续规划

1. 接入持续集成（CI），默认集作为提交门禁；
2. 扩充故障注入覆盖（网络中断、参数越界、多节点同时失效）；
3. 补充真机（SDK 仿真环境）回归测试，覆盖附录 B 的驱动链路。
# 14 已知问题与风险

本章汇总系统级问题、风险与排查经验。各子系统内部的具体限制见相应章节末尾。

## 14.1 环境相关问题

1. **环境适配参数较多**：`ROS_LOCALHOST_ONLY`、软渲染、关闭相机渲染、模型库路径等均为当前运行环境的适配措施而非业务逻辑。更换部署环境（真机、多机、带 GPU 主机）时需逐项评估：多机场景须移除回环 DDS 限制并配置正常发现机制，需要视觉功能时须去除关闭相机渲染的注入；
2. **发现机制不稳**：运行环境下存在节点偶发发现失败（估计节点启动失败导致控制开环、规划节点偶发卡死、定位节点偶发被生命周期管理误杀），多为环境性，重启 bringup 通常自愈，属于长期运行的已知痛点；
3. **启动时序为经验值**：固定时延只覆盖常见场景，慢场景下存在短暂降级窗口（见 12.2 节）。

## 14.2 系统层面问题

1. **导航到位精度受限**：轮子打滑导致定位漂移，导航基于里程计判定到位、定位基于地图系，端到端存在约 0.4 m 偏差。改善方向：定位权重调优、里程计航向比例标定、更换更稳健的轮子碰撞原语；
2. **轮式里程计精度受限**：全向轮打滑造成侧向速度残差与航向漂移，改善摩擦模型后仍存在；
3. **受限环境下图形显示的稳定性**：软件渲染下 rviz2 存在两类已知崩溃（带真实数据的栅格类显示触发着色器链接失败、工具配置为空时按键事件空指针），已通过禁用相关显示项与保留工具列表修复；受限环境下仍可能偶发，需重试或改用带 GPU 的环境；
4. **动力学仿真未入闭环**：力控方案仅用于调试，尚未接入闭环控制链路。

## 14.3 真机方向风险（详见附录 B）

1. **标定依赖**：真机投入前需标定里程计比例、复核旋转方向、验证位姿逆运动学可达性；
2. **传感器受限**：SDK 经网络仅回报关节与底盘状态，相机、雷达、IMU 暂无数据，健康话题相应不可用属预期；视觉与激光功能需另行接入传感器驱动；
3. **执行结果判定**：SDK 返回的是目标状态而非实际状态，判定执行结果须以仿真界面或真机实际运动为准，不能只依据状态回显；
4. **接口能力约束**：仅暴露关节空间控制、底盘三模式与夹爪布尔量，直线 / 圆弧轨迹等不可用接口不暴露给上层（依据附录 A 的测试结论）。

## 14.4 常见问题与排查

1. **从 Windows 调用 WSL 命令路径被转换**：Git Bash / MSYS2 会把 `/home/...` 转为 Windows 路径，调用前加 `MSYS2_ARG_CONV_EXCL="*"`；
2. **残留仿真进程导致二次启动报实体已存在**：正常退出已自动清理；异常退出后使用一键清场工具，或按进程名精确清理 gzserver / gzclient；
3. **控制器加载慢**：必须轮询控制器状态直到全部 active，不得提前下发命令；
4. **进程名匹配误杀**：`pkill -f` 会匹配命令行中的同名模式（含执行脚本自身），使用字符类技巧或按进程名精确匹配；
5. **命令行工具在负载下异常**：`ros2 node list` / `ros2 param get` 可能超时或上下文错误，执行 `ros2 daemon stop` 恢复；数据流验证优先使用脚本；
6. **订阅句柄未持有**：订阅接口返回的临时指针表达式结束即析构、订阅被销毁（表现为订阅计数为零、数据永远收不到），订阅句柄必须存成员变量；
7. **launch 参数名冲突**：被包含的子 launch 中同名参数声明会覆盖父层参数，新增子 launch 时参数改名隔离；
8. **共享内存锁占用端口**：残留锁文件会占 Fast DDS 端口导致规划节点初始化失败，清理共享内存锁后重启；
9. **跨 DDS 组不可见**：单独启动的进程未设置回环 DDS 变量时与 bringup 树互相不可见，所有单独启动的进程须同设（见 2.2 节）；
10. **修改后"未生效"**：优先怀疑安装目录旧副本（见 2.3 节重编规则），对比安装目录与源码目录对应文件。

## 14.5 后续工作方向

1. **视觉抓取**：开启相机链路，接入视觉感知，决策层编排抓取序列；
2. **动力学与关节驱动**：力控方案接入闭环，完善关节限位保护与力矩环（真机适用）；
3. **整机运动协调**：底盘与双臂协同（边转边平移分解、多段路径等）；
4. **导航精度调优**：定位权重标定、里程计标定、更换碰撞原语、局部代价地图稳定性；
5. **测试与部署扩展**：持续集成门禁、真机回归、多机场景验证。

# 附录

## A\. 原始资料与二次开发成果

附录 A 记录二次开发的完整输入与加工过程：先盘点厂家随设备提供的原始开源资料（A.1，归档于 `TR-RD28&&YL-R1D - 副本/`），再说明模型、参数、设计、测试、建模与验证等加工成果（A.2，归档于 `TR-RD28&&YL-R1D - 开发资料/`），最后给出结论与后续工作（A.3）。原始资料存在内容重复、资源冗余、信息分散等问题，本项目前期对其进行了整理，并在此基础上完成加工、转换与验证。

| 章节 | 内容 | 归档位置 |
|---|---|---|
| A.1 原始内容 | 厂家资料盘点（技术说明书/网络配置/模型/仿真/SDK 与例程） | `TR-RD28&&YL-R1D - 副本/` |
| A.2 处理过的内容 | 模型、参数、设计、测试、建模、验证 | `TR-RD28&&YL-R1D - 开发资料/` |
| A.3 结论与后续工作 | 可复用/必须重做/已知问题/后续工作 | — |

本报告及附录 A 的原始资料与开发成果（`TR-RD28&&YL-R1D - 副本/`、`TR-RD28&&YL-R1D - 开发资料/`）归档仓库：[Zhengshuji/YLR1D_controller-collection](https://github.com/Zhengshuji/YLR1D_controller-collection)。

### A\.1 原始内容

厂家提供的原始资料可分为五大类：技术说明书（开发手册）、网络配置文档、设备模型文件、仿真环境及上位机程序、代码及例程，构成二次开发的核心前置知识。

**处理前的厂家原始资料**见百度网盘（链接：https://pan.baidu.com/s/1sfX1zpC65LKl5oErayX1CA ，提取码：`7vf1`），为未加工的原始内容，需自行下载并提取以下部分后使用：

1. **技术说明书（开发手册）**：12 份（设备总体/配置/连接/TR-Pad 使用、Linux/ROS 基础、运动控制/传感器接口、SLAM/导航/图像识别/手眼标定）；
2. **网络配置文档**（"其他文档"目录）：4 份（机器人网络配置、外接路由、nano 共享 Windows 网盘、遥控器设备）；
3. **SDK 包**（"SDK及代码例程"目录）：`RobotConSys_SDK`（include / lib / example，四个平台预编译库）；
4. **应用例程**（"YL-R1D应用案例"目录）：ROS1 导航控制系统（ros_r1d_device / ros_r1d_nav）；
5. **仿真环境与上位机**（"配套软件、工具"目录）：TR-Sim（Unity 仿真）、TR-Pad（上位机）及远程工具；
6. **设备图片**（"图片"目录）与设备说明书、技术参数清单等根目录文档。

> 注：网盘中"设备模型文件"目录为空，原始 STEP 三维模型（`（标准-外发模型）TR-双臂全向移动复合机器人-0305.STEP`，约 190 MB）如需使用请另行获取，或使用本仓库开发资料中已处理完成的模型资产（A.2.1）。

#### A\.1\.1 技术说明书（开发手册）

这是一套按"认识设备 → 搭建环境 → 开发接口 → 实现功能"结构组织的开发手册，共 12 份，主要包含：

- **第 1 章（设备）**：设备总体介绍、设备配置介绍、连接与使用、TR-Pad 使用简介——回答"设备是什么、有哪些部件、如何开机与连接"；
- **第 2 章（环境）**：Linux 基础课程、ROS 基础课程——为没有 ROS 经验的后续开发者补齐环境知识（文档编号不连续，如 2.1 后直接为 2.5，系厂家原始编号）；
- **第 3 章（接口）**：运动控制接口、传感器接口——**与 SDK 直接对应**，是开发中最重要的两份文档；
- **第 4 章（功能）**：SLAM 建图、自主导航、图像识别、手眼标定——以例程为载体讲解各功能如何实现。

#### A\.1\.2 网络配置文档

机器人本体是一台 ROS 主机，**网络是一切联调工作的前提**。这四份文档解决了开发环境的网络问题：

- 机器人网络配置——机器人本体的基本网络参数；
- 机器人外接路由说明——开发机与机器人通过外接路由器互联；
- 机器人 nano 共享 windows 网盘——方便在 Windows 与机器人之间传输代码与地图；
- 遥控器设备——遥控器与设备绑定、用于手动控制。

#### A\.1\.3 设备模型文件

厂家提供的外发模型为 STEP 格式的三维模型（`（标准-外发模型）TR-双臂全向移动复合机器人-0305.STEP`，约 190MB，存放于 `副本/设备模型文件/`）。该模型**只有几何外形，没有任何运动学标注**——没有关节定义、坐标系或质量信息，因此不能直接用于仿真与规划，需要在此基础上自行加工得到可用的 URDF/XACRO（处理过程见 A.2.1）。

#### A\.1\.4 仿真环境及上位机程序

厂家用 **Unity** 编写了仿真环境 TR-Sim 与上位机 TR-Pad，二者与 SDK 包**使用同一套动态库、精准对接**。这意味着在没有实体设备的情况下，也可以完成大部分开发与联调工作：

- **TR-Sim**：Unity 3D 仿真环境，内置 SDK 动态库（`RobotConSys_Client.dll`、`VehicleMotionDev*.dll`、`TCPConDev*.dll` 等），可通过与真实机器人完全相同的接口下发指令、获取状态；
- **TR-Pad**：配套上位机（人机交互界面），用于配置设备、下发指令、查看状态；
- 厂家同时提供了 TR-Sim0819.zip（约 2.9GB）与 TR-Padv1.25.8.zip 安装包，便于环境部署。

本项目基于 SDK 进行了接口测试（见 A.2.4），测试环境配置见该节。

#### A\.1\.5 代码及例程

厂家提供了 **SDK 包** 与一套完整的 **应用例程**，分别对应"如何编程控制机器人"与"如何搭建一个完整系统"。

##### A\.1\.5\.1 SDK 包（RobotConSys_SDK）

SDK 包是**二次开发的核心资产、唯一不可替代的依赖**——所有对机器人的控制最终都经由它完成。它位于 `副本/代码及例程/RobotConSys_SDK/`，**实现全部为编译过的库（无源码）**，按四平台提供：`linux_aarch64`（Jetson 等 ARM64）、`linux_aarch64_u20`、`linux_x64`、`win_x64`。

接口按设备层组织，主要能力如下（依据 `example/RobotConSysDemo.cpp` 归纳）：

- **系统**：`init(ip, port)`、`close()`、`setAuthority()`；
- **机械臂**：伺服开关、关节空间绝对/相对运动、直线/圆弧/跳跃运动、回零、回原点、JOG/步进、等待指令完成、参数查询；
- **底盘**：控制类型设置、伺服开关、运动控制（平移/旋转/停车）、状态与 IMU 查询；
- **夹爪**：开关状态设置与查询；
- **相机**（CamDev_Client）：初始化、彩色/深度图像采集。

SDK 的使用流程是固定的：加载动态库 → 创建实例 → init 连接 → 设置权限 → 下发指令 → close 释放。`example/` 中提供了机械臂、底盘、相机三个示例（`RobotConSysDemo.cpp`、`CamDevDemo.cpp`）。

**平台与库路径的指定方式**：

- 编译目标平台在 `example/platform.cmake` 中通过 `TARGET_PLATFORM` 变量指定（`linux_aarch64` / `linux_x64` / `win_x64` 等），用于选择编译器与输出目录；
- **动态库的加载路径在实现文件中指定**：示例在 `RobotConSysDemo.cpp` 中通过 `SysLayer::CLoadLibrary` 运行时加载动态库（`libRobotConSys.open(".", "RobotConSys_Client")`），库路径为调用处指定的路径（示例为当前目录 `"."`），而非通过构建脚本传入。

```bash
cd example && mkdir -p build && cd build
cmake -DTARGET_PLATFORM=linux_aarch64 ..   # 或 linux_x64 / win_x64 / linux_arm
make
```

正文 translate 层（第 7 章）定位为"与 SDK 包对齐"，即围绕 SDK 接口实现。

##### A\.1\.5\.2 例程（YL-R1D应用案例）

厂家还提供了一套完整的 **ROS1 导航控制系统例程**（`副本/代码及例程/YL-R1D应用案例/`）。它基于已过时的 ROS1，但展示了如何用 SDK 搭建一个完整的机器人系统：

- **设备层**（`ros_r1d_device`）：启动管理（launch）、**SDK 封装包 `robot_package`**（将机械臂/底盘/夹爪封装为 ROS 话题、10 个服务与 MoveABSJ 动作）、传感器驱动（RealSense 相机、YDLidar 雷达、Hipnuc 串口 IMU/GNSS）与视觉依赖；
- **上层**（`ros_r1d_nav`）：启动编排（bringup）、控制（机械臂、贴墙对齐）、定位建图（amcl、ekf、gmapping）、感知（相机节点与标定）、规划（多点导航、move_base 系）、任务控制（TCP 服务端）与 YOLOv8 视觉识别。

其中多数传感器驱动与算法为**开源社区项目**，上游仓库可直接获取：RealSense 相机驱动（[IntelRealSense/realsense-ros](https://github.com/IntelRealSense/realsense-ros)）、YDLidar 雷达驱动（[YDLIDAR/ydlidar_ros_driver](https://github.com/YDLIDAR/ydlidar_ros_driver)）、YOLOv8 识别（基于 [Ultralytics](https://github.com/ultralytics/ultralytics) 的 TensorRT 推理）；Hipnuc 串口 IMU/GNSS 为厂家配套驱动。

该例程对理解"如何用 SDK 搭一个完整机器人系统"的价值在于：

1. 传感器配有**标定程序**（相机标定 `camera_calibration`、里程计标定 `odom_calibrator`）；
2. 完整**揭示了外部传感器与外部算法的依赖**（RealSense、YDLidar、Hipnuc、YOLOv8、move_base 导航栈）；
3. 提供了 **SDK 封装的完整实现**（`robot_package`：将机械臂/底盘/夹爪接口封装为 ROS 话题、10 个服务与 MoveABSJ 动作），可直接作为封装工作的参照；
4. 同时暴露了**需要重写的内容**：SDK 自带的 TCP 通信已过时（`task_controller/TCP_SDK_old` 为旧版实现）、ROS1 框架整体过时。

其"建图 → 保存 → 导航 → 打点 → 执行任务"的操作流程见 `文档说明/操作手册.md`。

#### A\.1\.6 配套软件、工具与设备图片

除上述五类资料外，厂家还提供了开发过程中所需的配套软件、工具与设备图片：

- **远程工具**：MobaXterm（远程终端）、NoMachine（远程桌面）——用于远程登录机器人进行开发与部署；
- **安装包**：TR-Sim0819.zip、TR-Padv1.25.8.zip、`RobotConSys_SDK.zip`、`robot_package.zip`——便于环境重装与分发；
- **设备图片**：YL-R1D 设备照片（存放于 `副本/图片/YL-R1D/`），可用于文档插图。

### A\.2 处理过的内容

原始资料存在如下问题：**模型没有运动学标注、参数缺失、SDK 自带 TCP 通信与 ROS1 例程框架过时**。针对这些问题，本项目进行了以下处理。

#### A\.2\.1 机器人模型

原始 STEP 模型无法直接用于仿真与规划，处理链路为：

1. **修复 STEP 乱码**（`TR-RD28&&YL-R1D - 开发资料/01_STEP模型处理/`）：用脚本 `extract_chinese_names.py` 提取 STEP 中的中文字符、生成 `translate.csv` 中英对照、`step_translate.py` 批量替换为英文，解决 STEP 文件的乱码问题并可在不同平台使用；
2. **SolidWorks 加工**（`TR-RD28&&YL-R1D - 开发资料/01_STEP模型处理/模型文件/modul_SW*`）：将 STEP 转为 SolidWorks 格式，添加参考几何体以支持 URDF 导出（含"加超声波传感器"变体）。SolidWorks 中的加工界面（装配树与参考几何体）见图 A-1。

![图 A-1 SolidWorks 模型加工界面](images/solidworks.png)

*图 A-1 SolidWorks 模型加工界面*
3. **URDF → XACRO 转换与参数化**（`TR-RD28&&YL-R1D - 开发资料/02_模型转换工具/`）：采用 [URDF2Xacro](https://github.com/OpenGHz/URDF2Xacro) 及其配套脚本将 SolidWorks 导出的 URDF 转 Xacro 并参数化，工具脚本归档于 `TR-RD28&&YL-R1D - 开发资料/02_模型转换工具/URDF2Xacro/`（含转换脚本、mesh 工具与配置示例）；同时评估了第三方工具 [urdf_from_step](https://github.com/ReconCycle/urdf_from_step)（STEP 直接转 URDF，依赖 pythonocc-core）及其移植的 [ROS2 版](https://github.com/Zhengshuji/urdf_from_step_ros2)；
4. **流水线化生成 ROS2 功能包**（`TR-RD28&&YL-R1D - 开发资料/03_模型流水线_SW转ROS2/urdf_ros2_rviz2/`）：将步骤 2-3 固化为自研 8 步流水线（清理→重命名→修复 URDF→提取传感器→生成配置→转 Xacro→添加 Gazebo 插件→打包），以 SolidWorks 导出的 URDF 为输入生成 ROS2 功能包，产出 **`ylr1d_description`**（urdf/xacro + meshes + config + launch + rviz，支持 RViz2 与 Gazebo）。该流水线代码开源在 [Zhengshuji/Model-Transformation-from-Urdf-Exported-by-SolidWorks](https://github.com/Zhengshuji/Model-Transformation-from-Urdf-Exported-by-SolidWorks)。

#### A\.2\.2 机器人参数

模型处理完成后，需要确定**运动学、动力学、传感器位姿**三类参数。参数通过工具 `xacro2dh`（`TR-RD28&&YL-R1D - 开发资料/02_模型转换工具/urdf2dh/xacro2dh/`）从 xacro 展开后自动提取，并以随机关节角做 FK 交叉验证（DH 结果 vs URDF 结果）检查一致性（`urdf2dh` 目录下另含第三方 [urdf2dh 库](https://github.com/takeshiD/urdf2dh) 作参照）。三份参数文档的完整内容见 `TR-RD28&&YL-R1D - 开发资料/02_模型转换工具/urdf2dh/xacro2dh/output/docs/`（与飞书文档同步），摘要如下。

**运动学参数（标准 DH，Craig 约定）**：共三条运动学链——

- `left_arm`：基座 `Link_Body2` → 末端 `Link_LeftArm7`，7 个旋转关节（`Joint_Body2_to_LeftArm1` … `Joint_LeftArm6_to_LeftArm7`）；
- `right_arm`：基座 `Link_Body2` → 末端 `Link_RightArm7`，7 个旋转关节；
- `body`：基座 `Link_Base` → 末端 `Link_Body4`，4 个关节（`Joint_Base_to_Body1` … `Joint_Body3_to_Body4`）。

正运动学采用 `FK = base_transform · Π A_i(q) · tool_transform`，各链 base/tool 变换与完整 DH 表（a、alpha、d、theta_offset 及关节限位）见 docs/运动学文档。文档标注：数值已按原数据保留有效精度，实际使用可适当截断；FK 与 URDF 逐点一致，可放心用于控制与仿真。

**动力学参数**：逐 link 给出质量、质心（COM）与惯量张量（定义在过质心、与 link 系平行的坐标系），单位 kg、m、kg·m²。要点：

- 底座 `Link_Base`：质量 16.208 kg，COM (0.0373, 0.0003, 0.2025)；
- 车身 `Link_Body1`：3.3193 kg；躯干其余三节 0.186–0.898 kg；
- 单臂 7 节质量 0.060–0.303 kg，两指各 0.0109 kg，左右臂对称；
- 车轮转向节 0.0653 kg、车轮 0.5905 kg（四轮一致）；
- 所有传感器 link 统一质量 0.001 kg、惯量对角 1e-06 kg·m²。

**传感器位姿**：以 `Link_Base` 为参考坐标系，逐传感器给出 `mount.transform`（相对父 link 的固定位姿）与 `pose_base`（零位形下相对基座系的位姿）。传感器清单：基座侧全局相机（彩色/深度/红外，共用 `Link_GlobalCameraSensor`，挂于 `Link_Body4`）、左/右手部相机（彩色/深度/红外各一，挂于各自 `Link_*Arm7`）、雷达（`Link_RadarSensor`，直接固定在基座）、IMU（`Link_IMUSensor`，固定在基座）、四角超声波（固定在基座）。挂在 DH 链末端的传感器（相机）在任意关节状态下的位姿由链 FK 计算，公式见 docs/传感器位姿文档。

**参考**：本地产出与在线文档对照如下（链接仅为在线引用，权威内容以本地 docs 与 YAML 为准）：

| 参数 | 本地文档（权威） | 在线文档（飞书） |
|---|---|---|
| 运动学（标准 DH） | `docs/机器人运动学参数文档（标准 DH）.md`、`output/kinematics.yaml` | [机器人运动学参数文档（标准 DH）](https://qcnkr8qd7w8a.feishu.cn/wiki/FFWrwPo7wit4NVkCt7LcTu0DnDd) |
| 动力学 | `docs/机器人动力学参数文档.md`、`output/dynamics.yaml` | [机器人动力学参数文档](https://qcnkr8qd7w8a.feishu.cn/wiki/EpqUw7PepiTG4NkGvsdcr5t0nMg) |
| 传感器位姿 | `docs/机器人传感器位姿参数文档.md`、`output/sensors.yaml` | [机器人传感器位姿参数文档](https://qcnkr8qd7w8a.feishu.cn/wiki/I2b1wUOkSikqzekH0FAcWZ6Vnig) |

#### A\.2\.3 控制系统设计文档

在参数确定后，形成了控制系统设计的总体文档（`TR-RD28&&YL-R1D - 开发资料/04_控制与仿真/robot_control/docs/YLR1D 机器人控制系统.md`），正文各功能包章节（第 3-13 章）的框架即来源于此，要点：

- **五层框架**：感知层（信息获取/预处理/消息派送/高级感知）、决策层（任务决策）、规划层（轨迹规划与导航，实际调用 SDK）、控制层（下位机多闭环）、硬件层（仿真时配置 Gazebo 参数）；
- **仿真系统设计**：物理层采用 xacro 模型，提供运动学仿真（控制位置，契合 SDK）与动力学仿真（控制力/力矩）；为降低开销使用 gzserver 不可视化；
- **控制层设计**：运动控制指令封装与发送，输入输出与 SDK 一致（拆分为躯干、左臂、右臂、底座四部分）；内部实现闭环 PID（可扩展为任意离散系统模型），含限幅与输入调整；夹爪用 0/1 控制、两指只需控制其一；
- **转译层设计**：与规划层/控制层之间采用 Action 通信；小车由八关节控制封装为运动方式+速度；夹爪由两关节变为一个布尔量；
- **规划层设计**：底座部分依赖 nav2、机械臂部分依赖 moveit2，生成可执行的关节空间序列；
- **模型修正记录**：文档记录了调试中发现并需修正的问题——左轮速度正时反转、左臂部分关节反向、右臂部分关节反向、夹爪问题受初始位置限制等。

该项目代码位于 [Zhengshuji/YLR1D_Controller](https://github.com/Zhengshuji/YLR1D_Controller)（分支 `model_fix`）。

#### A\.2\.4 SDK 包测试项目

对 SDK 接口进行了系统测试，测试工程位于 WSL 工作区 `\\wsl.localhost\Ubuntu-22.04\home\zsj\WorkSpace\YLR1D\robot_package\RobotConSys_SDK\example`（基于 `RobotConSysDemo.cpp` 的扩展测试版，覆盖机械臂、底座、夹爪、错误码等用例）；测试结论整理为说明文档（本地归档 `TR-RD28&&YL-R1D - 开发资料/02_模型转换工具/urdf2dh/xacro2dh/output/docs/YL-R1D说明文档（SDK接口与测试）.md`，在线引用：[YL-R1D 说明文档](https://qcnkr8qd7w8a.feishu.cn/wiki/F5ljwZO4Ziw9uHkl4U3csvC5nhc)）。要点如下。

**数据类型**：SDK 主要提供 `Joints`（关节）与 `Terminal`（末端）两个变量类型及对应的数组类型，声明于 `include/RobSoft/CDataStructure.hpp`（同时含四元数、向量及运算）；建议使用 INT、DOUBLE、BOOL 作为其它变量类型。

**接口能力**（依 RobotConSys 类归纳）：

- 系统操作：init/close、权限查询与设置、参数/配置/坐标系读写、工具/工件坐标系修改、TCP/TCF/USRF 标定、系统状态查询；
- 运动控制：归零/回原点；点动（jointJOG/terminalJOG/armAngleJOG）、按速率点动；步进（jointStep/terminalStep/armAngleStep）；关节空间规划（moveABSJoint/moveABSJointR）；末端空间规划（moveJoint/moveJointR）；末端轨迹（直线、圆弧、B 样条、点列、连续轨迹）；力矩控制（零力拖动示教、力传感器柔顺）；视觉伺服；夹爪控制；底座控制；
- 外部接口：本体/外部 IO、modbus 寄存器、外部 TCP 设备、程序执行。

**测试中发现的问题**（重要，直接影响 translate 层接口选取）：

- `terminalJOG` 会产生严重错误，导致系统断开连接；`terminalStep` 极易报错、可能导致断开；`armAngleJOG`/`armAngleStep` 无效果；
- 直线、圆弧轨迹规划**无法使用**；
- 传感器方面：SDK 通过 TCP/IP 直接获取的传感器**不包含 IMU、超声波、雷达**，视觉图像可以正常读取；
- 链接问题：编译产物为 .so 共享对象，运行时可能出现找不到库（`libSystemLayer.so` 等）或函数无法调用（`undefined symbol: createRobotConSys_Client`）的错误，解决方法是将库路径加入 `LD_LIBRARY_PATH` 或在程序路径下创建软链接。

**测试环境**：WSL 配置网络模式为镜像，IP 为 127.0.0.1 回环地址，常用测试指令（nc/curl/Test-NetConnection）见说明文档第 8 节。

**其它参数**（说明文档第 7 节）：机械臂 7 关节范围（如 J1 ±150°、J2 −90°~105°）、躯体 4 关节范围（含移动关节 ±300mm）、完整错误码表（如 ERROR_NO_INVERSE_KINEMATICS、ERROR_OVERRANGE、ERROR_SERVO_OFF 等）。

> 说明：上述测试结论与正文 translate 层（第 7 章）的接口设计一致——机械臂仅选取可用的关节空间控制、底座仅保留平移/旋转/停车三种模式、夹爪用布尔量控制。

#### A\.2\.5 被控对象数学建模

对被控对象（底座、机械臂、夹爪）进行数学建模分析，作为控制系统设计（正文各层）的依据。完整内容见本地 `docs/被控对象数学建模.md`（与飞书文档同步，在线引用：[被控对象数学建模](https://qcnkr8qd7w8a.feishu.cn/wiki/OzYtwg9zei3K6LkJFtecun2jnrh)），要点如下。

**底座模型**：四轮转向关节均为绕 Z 轴的 revolute 关节、车轮驱动关节为 continuous；内部参数——左右轮间距 d = 0.320 m、前后轮间距 l = 0.426 m、轮径 r = 0.0775 m。静态运动学分析假设纯滚动无侧滑，由轮子接地点速度与转向角建立 `AX = b` 关系解算车体速度 (Vx, Vy, ω)。

**SDK 底座运动模式**（建模时特别关注）：SDK 仅提供少数固定运动模式——

1. 平移：四轮朝向一致为前进方向，速度一致为期望速度；
2. 原地旋转：轮向固定为 `[2.2150, 0.9265, -2.2150, -0.9265]`，且 ω = 3.7538·v；
3. 锁死：轮速为 0，两对角轮方向平行，轮向固定为 `[-0.6443, 0.6443, -2.4973, 2.4973]`；

且总是先完成转向、再进行移动。

**机械臂模型**：采用 DH 参数建模（关节角/速度/限位参数见 A.2.2 运动学文档）；外部输入（底座传导的速度、加速度、外力扰动）需估计。

**机械夹爪**：左爪行程 [-0.014, 0]，右爪行程 [0, 0.014]；Finger1 与 Finger2 绕 Z 相差约 180°，两指运动方向总相反——因此控制一个即可（正文 translate 层即用布尔量控制）。

**建模结论**（对控制系统设计的要求）：SDK 仅提供基于运动学的控制，控制器设计应以前馈为主、反馈为辅；SDK 各部分相对分离，需重点处理部位间的配合关系；SDK 内传感器相对受限，建议增加状态估计器。

#### A\.2\.6 控制系统验证（三层控制架构）

在控制系统设计文档（A.2.3）的基础上，形成了**三层控制架构**的实现与验证（代码位于 `TR-RD28&&YL-R1D - 开发资料/04_控制与仿真/robot_control/`，即 A.2.3 所述仓库的一部分，架构说明见其 `ARCHITECTURE.md`）：

- **上层（运动学/规划层 `ylr1d_upper_control`，待实现）**：底盘麦克纳姆轮正逆解、双臂 IK（MoveIt2 或自建求解器）、躯干运动学（升降+偏航+俯仰耦合）、末端笛卡尔空间路径规划、整身协调；
- **中层（指令转发层 `ylr1d_mid_control`，已实现并验证）**：基于 ros2_control + ForwardCommandController，共 6 个控制器（底盘转向 4、底盘驱动 4、躯干 4、左臂 9、右臂 9 关节 + joint_state_broadcaster），合计 30 个可控关节；在 Gazebo Classic 中通过 Topic 发布 `Float64MultiArray` 可控制全部关节；
- **下层（关节驱动层 `ylr1d_lower_control`，待实现）**：每个关节的 PD 位置/速度闭环、重力补偿、关节限位保护、力矩/电流环。

验证环境：ROS2 Humble、WSL Ubuntu 22.04、Gazebo Classic 11（启动与测试命令见 `ARCHITECTURE.md` 的快速开始）。中层验证过程中解决的环境配置问题见 A.3。

### A\.3 原始项目的价值、已知问题与后续工作

**可直接复用**：SDK 包（硬依赖，唯一的底层控制入口）、TR-Sim 仿真环境（无实体联调与测试平台）、例程的思想与范式（传感器标定、SDK 封装、外部依赖清单）。

**需要重做/已重做**：SDK 自带 TCP 通信已过时、ROS1 例程框架已过时（`task_controller/TCP_SDK_old` 为旧版实现）；模型与参数在原始资料中缺失，已在 A.2.1/A.2.2 中补齐。

**已知问题与踩坑记录**（供后续开发参考，避免重复排查）：

1. SDK 接口（详见 A.2.4 与说明文档）：`terminalJOG` 会致系统断开连接、`terminalStep` 极易报错、`armAngleJOG/Step` 无效果；直线/圆弧轨迹规划无法使用；TCP/IP 直接获取的传感器不含 IMU、超声波、雷达；编译产物为 .so，运行时可能找不到库或函数无法调用（需设置 `LD_LIBRARY_PATH` 或在程序路径下建软链接）；
2. 中层控制验证（详见 `robot_control/src/ylr1d_mid_control/README.md`）：gazebo_ros2_control 插件 CLI 参数解析失败（XML 特殊字符）、`LD_LIBRARY_PATH` 未含 `/opt/ros/humble/lib` 致插件不加载、控制器 type 参数未定义、joints 参数为空致控制器 unconfigured——均已解决并记录修复方式；
3. 模型方向修正（A.2.3）：左轮速度正时反转、左右臂部分关节反向、夹爪受初始位置限制。

**后续工作**（待实现，见 `robot_control/ARCHITECTURE.md`）：

1. 实现上层运动学（底盘 + 双臂）；
2. 实现下层关节驱动（真机适用）；
3. 整机运动协调控制；
4. 附录 B：SDK 封装为 ROS2 节点并接入控制系统（已完成，见附录 B）；
5. 增加状态估计器（被控对象建模结论建议，见 A.2.5）。

**开发环境**：

- 控制系统：ROS2 Humble、WSL Ubuntu 22.04、Gazebo Classic 11，构建工具 colcon；
- SDK 示例：依赖 Eigen（头文件自带）与 OpenCV，编译产物为共享库（.so）；
- 模型流水线：Python 3.8+、PyYAML、ROS2、Gazebo；
- 参数提取（xacro2dh/xacro2urdf）：Python 3.8+、numpy、PyYAML。

**自研代码仓库**：

- 模型流水线：[Zhengshuji/Model-Transformation-from-Urdf-Exported-by-SolidWorks](https://github.com/Zhengshuji/Model-Transformation-from-Urdf-Exported-by-SolidWorks)
- urdf_from_step ROS2 移植版：[Zhengshuji/urdf_from_step_ros2](https://github.com/Zhengshuji/urdf_from_step_ros2)
- 控制系统：[Zhengshuji/YLR1D_Controller](https://github.com/Zhengshuji/YLR1D_Controller)（分支 `model_fix`）

**开发入口**：原始资料见 `TR-RD28&&YL-R1D - 副本/`（各目录含 README）；处理过程与工具见 `TR-RD28&&YL-R1D - 开发资料/`（各主题目录含 README）。

## B\. SDK 封装与真机控制系统

正文各章的各层框架基于 Gazebo 仿真（YLR1D_Controller）搭建，其 translate 层（第 7 章）定位为"与 SDK 包对齐"。附录 B 完成对齐的落地：把厂家 SDK（RobotConSys_Client）**封装为 ROS2 节点**（B.1），并**接入正文搭建的控制系统框架**（B.2），在 SDK 自带仿真（TR-Sim，与真机接口一致）上完成全链路实测。

代码位于 WSL 工作区 `YLR1D_ROS2`（`\\wsl.localhost\Ubuntu-22.04\home\zsj\WorkSpace\YLR1D_ROS2`）：自研包 `robot_interfaces`/`robot_package`/`robot_driver`/`robot_sensors` 与 `bringup` 位于 `src/`，框架（YLR1D_Controller 子模块）位于 `src/ThirdParty/`；RobotConSys 控制器地址 `172.22.224.1:8109`。真机控制系统（附录 B 内容）代码仓库：[Zhengshuji/YLR1D_Controller_SDK](https://github.com/Zhengshuji/YLR1D_Controller_SDK)。**总体策略**：框架主体（感知/规划/moveit/HMI/nav2/决策层）原样复用，**驱动层平替「转译＋控制＋算法＋plant」整段**——3 个转译 action（复用 `ylr1d_translate` 类型）＋内联主线程 SDK 调用＋度/弧度换算＋三伺服使能；传感器层把 SDK 状态桥接为框架 30 关节 `/joint_states`，上层零感知差异。

### B\.1 SDK包封装为ROS2节点

封装工作由四个包完成：接口包 `robot_interfaces`（自建消息/动作）、SDK 驱动包 `robot_package`（SDK 库＋封装测试 Demo）、驱动层 `robot_driver`（3 action＋SDK 直调）、传感器层 `robot_sensors`（SDK 状态 → 框架关节）。

下面给出示意图：
```mermaid
flowchart TD
    OUT[外部任务] --> UP[上层] --> DWON1[下层-仿真] &  DOWN2[下层-实际] --> PER[感知层]
    PER --->  DOWN1 & DOWN2
    PER ---->  UP
subgraph UP
    决策层 --> 规划层
end
subgraph DOWN1
    转译层 --> 控制层 <--> 算法层
    控制层 --> 仿真层 --> 仿真系统
end
subgraph DOWN2
    SDK封装包 --> 实际系统
end
```
*示意图*

#### B\.1\.1 封装策略：dlopen 工厂＋薄封装类

SDK 全部为编译产物（无源码）：`libRobotConSys_Client.so`、`libRobSoft.so`、`libSystemLayer.so`、`libCamDev_Client.so`、`libTCPConDev_Client.so`、`libmodbus*.so`。封装沿用厂家 Demo 的 **dlopen 工厂模式**：`SysLayer::CLoadLibrary` 运行时加载动态库 → `loadFunc("createRobotConSys_Client")` 创建实例 → `init(ip, port)` 连接 → `setAuthority` 设权限 → 下发指令 → `free` 释放。

在此基础上封装为薄类 `RobotSdk`（`robot_driver/include/robot_driver/robot_sdk.hpp`），**所有 SDK 调用收口在此类**，驱动节点只依赖它：

- **单位换算在 SDK 边界统一完成**（实测约定）：臂关节角=**度**、末端位置=**mm**/姿态=**度**、底盘 vx/vy=**mm/s**、wz=**rad/s**；框架/moveit 用弧度＋米 → 下发 rad→deg、上报 deg→rad（mm→m）；
- **库路径由可执行文件位置推算**（`/proc/self/exe` → `install/robot_driver/lib`），不依赖 cwd，规避 A.2.4 的"找不到库"问题；运行库随包安装并以 `$ORIGIN/../lib` RPATH 命中；
- 连接生命周期、伺服使能、权限、结构探测等逻辑均收口在类内。

#### B\.1\.2 接口包 robot_interfaces

自建接口包（`rosidl_generate_interfaces` 自动发现构建 msg/srv/action）：

- **msg**：`TRArmMsg`（关节角＋末端位姿＋伺服/夹爪状态）、`TRVehicleMsg`（底盘速度/轮速/舵角/超声）、`TRVehicleIMUMsg`；
- **action**：`ArmMoveABSJ`（arm_index＋joints＋vel，对应 SDK `moveABSJoint`）；
- **srv**：按 A.1.5.2 例程 `robot_package` 的 10 个服务迁移对齐（`MoveABSJ`/`MoveJ`/`MoveL`/`Claw`/`Servo`/`VehicleControl`/`VehicleServo`/`VehicleCommModel`/`CalculateCoordinate`/`ImageToFrame`），保留供后续扩展。

**驱动层 3 个 action 复用框架 `ylr1d_translate` 类型**（`ChassisMove`/`ArmMove`/`GripperMove`）——话题名、消息结构与仿真栈完全一致，规划层/HMI/决策层对接零改动（这是 B.2 接入控制系统的关键）。

#### B\.1\.3 SDK 驱动包 robot_package

随包携带 SDK 库与头文件（`install/robot_package/lib`），提供 4 个可执行文件作为 **SDK 封装测试的载体与权威参照**：

- `armDemo`：机械臂测试序列（伺服使能 → moveABSJoint → waitMotionCMDFinish → returnZero），20Hz 轮询发布 `/sensors/arm_raw`；
- `vehicleDemo`：底盘测试序列（前进 → 自旋 → 停车），发布 `/sensors/vehicle_raw`，含"SDK 上报恒 0 时回落命令速度"的判定逻辑；
- `camDemo`：相机图像采集（CamDev 库＋OpenCV）；
- `test_node`：最小连通性测试。

Demo 能在 SDK 仿真界面真实驱动设备，是排查驱动层问题的**权威参照**（驱动代码与其逐行对齐，见 B.2.2 根因 1）。

#### B\.1\.4 驱动层 robot_driver

`robot_driver` 节点是 SDK 封装的对外出口，**平替「转译＋控制＋算法＋plant」**：

- **3 个 action server**（话题名与转译层一致）：`/chassis_move`（mode/direction/speed/duration）→ `setMotionControl`；`/arm_move`（part＋positions）→ `moveABSJoint`＋`waitMotionCMDFinish`；`/gripper_move`（part＋open）→ `setClawState`；
- **20Hz 定时器动作状态机**（同转译层 translate_node 模式），不阻塞 executor；臂运动按线程亲和约束内联主线程执行（B.2.2 根因 1）；
- **状态轮询发布**：`/sensors/arm_raw`（双臂＋躯干，arm0/arm1/arm2）＋`/sensors/vehicle_raw`（底盘，速度按最后命令死推）供传感器层；`/health` 自维护（`OK`/`LOST_CONNECTION`/`NOT_CONNECTED`）；
- **连接生命周期**：2s 断线重连、结构探测（ARM_1..3 的 DOF/伺服/错误码）、三组伺服使能、权限 OPERATOR、可选启动回零；
- **底盘模式**：平移用 `MOVE_XY`（全向 vx/vy，实测 NORMAL 只认 vx/wz、纯横移不动）、旋转 `ROTATE`、停车 `NORMAL(0,0,0)`——与 A.2.5 建模的 SDK 三模式一致。

#### B\.1\.5 传感器层 robot_sensors

- `sensor_bridge`：订阅 `/sensors/arm_raw`＋`/sensors/vehicle_raw`，组装为**框架 30 关节名 `/joint_states`**（转向=servo_pos、轮=wheel_vel、臂=ARM_1/2、躯干=ARM_3、夹指=夹爪开合映射），关节名单一来源 `ylr1d_description/config/joint_config.hpp`；
- `odom_pub`：**命令速度死推**积分（SDK `getVehicleState` 不回报实际速度）→ `/odom`＋odom→Link_Base TF（真机无 EKF，本节点补齐里程计）。

### B\.2 接入控制系统

#### B\.2\.1 接入架构

真机系统与框架的关系：**真机系统 = 框架的「规划层＋感知层＋HMI＋决策层」原样复用，把「转译层下游的仿真执行链」替换为 SDK 直驱，中间加一层「传感器桥」**（SDK 状态 → 框架 30 关节）。两个"共同语言"保证上层零感知差异：

- **3 个转译 action**（`/chassis_move` `/arm_move` `/gripper_move`）：moveit_bridge/goal_server、nav2 cmd_vel_bridge、决策层 plan_client 全部原样对接；
- **`/joint_states` 30 关节**：感知层（清洗/估计/里程计）、rsp TF、HMI 与仿真栈输入一致。

真机系统的数据流与复用关系见图 B-1。

```mermaid
flowchart TD
    DEC[决策层 ylr1d_decision<br/>原样复用] -->|/navigate_to_pose<br/>/plan/moveit/moveit_move| PLN[规划层<br/>nav2 + moveit2 原样复用]
    PLN -->|3 个转译 action<br/>/chassis_move /arm_move /gripper_move| DRV[驱动层 robot_driver<br/>平替转译+控制+算法+plant]
    DRV -->|SDK 直调| SDK[RobotConSys 服务端<br/>真机 / SDK 仿真]
    SDK -->|关节+底盘状态| SENS[传感器层 robot_sensors<br/>sensor_bridge + odom_pub]
    SENS -->|30 关节 /joint_states| PER[感知层 ylr1d_perception<br/>原样复用]
    PER -->|/perception/*| DEC
    HMI[ylr1d_hmi<br/>原样复用] -.-> PLN
```

*图 B-1 真机系统架构与数据流*

与正文层级对照：

| 正文层级 | 仿真栈（正文第 3-13 章） | 真机栈（本附录） |
|---|---|---|
| 决策层 | ylr1d_decision | **原样复用** |
| 规划层 | nav2＋moveit2 | **原样复用**（真机参数微调，B.2.2） |
| 转译层 | 转译 action → /desired_joint_states | **驱动层直调 SDK**（接口仍为同一 3 action） |
| 控制/算法层 | 采样保持＋PID＋A-lite 预测器 | **不存在**（闭环在 SDK/真机控制器内） |
| 仿真层 | Gazebo＋plant_sim | **不存在**（TR-Sim/真机） |
| 感知层 | ylr1d_perception | **原样复用**（输入换成传感器桥 /joint_states） |

#### B\.2\.2 关键适配点（实测根因，均已修复）

1. **SDK 指令线程亲和**（根因 1）：`moveABSJoint`＋`waitMotionCMDFinish` 必须在**发起连接的线程**（executor/主线程）内联执行；worker 线程发出会被服务端忽略/卡死（armDemo 能动正因为指令在 main 线程）。修复：驱动内联主线程执行，回零/臂运动共用。
2. **单位制**（根因 2）：弧度当度发 → 肉眼不可见；SDK 的度当弧度喂 moveit → OMPL 报 invalid bounds（早期"越限"假象）。修复：`robot_sdk.hpp` 边界统一换算（B.1.1）。
3. **底盘无速度反馈**（根因 3）：`getVehicleState` 恒 0 → 里程计按**最后命令速度死推**（仿真忠实于命令，死推与实动一致；真机需标定比例）。
4. **伺服使能**：三组伺服（ARM_1 左臂/ARM_2 右臂/**ARM_3 躯干**）必须 `setServoState(ON)`（含 ARM_3，否则躯干 moveABSJoint 返回 -1）；权限用 OPERATOR（实测 ROOT 下 armDemo 序列臂不动）。
5. **结构映射**：SDK 探测确认 ARM_1 左臂(7)、ARM_2 右臂(7)、ARM_3 躯干(4)；**ARM_4 探测会卡死服务端（勿调用）**；`part 0→ARM_3 / 1→ARM_1 / 2→ARM_2`。
6. **规划层适配**：全栈 `use_sim_time=false`（墙钟，无 /clock）；无激光 → **无 amcl，静态 map=odom**（恒等 TF），地图用 nav_test；moveit_bridge 服务等待重试 180s、moveit_goal_server 段超时 240s（躯干大轨迹 ~178° 瞄准 >90s）；cmd_vel_bridge 速度放大 1.0（真机命令即真实速度）、wz 直通（SDK +wz=逆时针，与 ROS 约定一致）。
7. **进程一致性**：顶层统一注入 `ROS_LOCALHOST_ONLY=1`（loopback DDS，绕开 WSL multicast 慢发现）；单独启动的 CLI/脚本须带同一环境变量，否则跨组互相看不见。

#### B\.2\.3 启动方式（bringup 包）

```bash
ros2 launch bringup real_robot.launch.py          # 基线：驱动＋传感器＋感知＋rsp
ros2 launch bringup real_robot_manual.launch.py   # ＋hmi_translate（手动 3 action 直发）
ros2 launch bringup real_robot_nav.launch.py      # ＋导航核心＋hmi_plan（NavigateToPose）
ros2 launch bringup real_robot_moveit.launch.py   # ＋moveit＋HMI（等 1-2 分钟 move_group）
ros2 launch bringup real_robot_decision.launch.py # 决策层全栈一键：nav_core＋moveit＋决策＋HMI＋单一 rviz
```

导航核心抽为 `real_robot_nav_core.launch.py`（基线＋odom_pub＋map_server＋静态 map=odom＋nav2 核心＋cmd_vel_bridge），供 nav/decision 两 bringup 共用。

#### B\.2\.4 测试验证（SDK 仿真实测）

真机链路验证脚本与运行日志位于 WSL 开发环境（临时目录，归档时未保留），实测结果如下：

**驱动层**（verify_all.sh 全链路）：底盘平移/原地旋转/横移(MOVE_XY)、左臂/右臂/躯干关节运动、夹爪开合**全部 SUCCEEDED**；关节回显与弧度目标一致（左臂 0.3/-0.2/0.1）；`/joint_states` 30 关节 20-33Hz；结构探测 ARM_1 DOF=7 / ARM_2 DOF=7 / ARM_3 DOF=4，三组伺服使能。

**moveit**：臂关节目标（OMPL 规划 → waypoint → 驱动 → 仿真真实转动）、臂姿态目标（IK）、躯干瞄准（~178° 回转＋俯仰）、夹爪段**全链路 SUCCEEDED**。

**nav**：`NavigateToPose` SUCCESS，底盘实动（/odom 死推位姿随动）。

**决策层**（Mission → BT → plan_client → 规划层 → 驱动 → SDK）：三演示任务实测：

| 任务 | 参数 | 结果 |
|---|---|---|
| arm_move | part=1，+x 5cm | ✅ outcome=0：moveit 规划 10 点 → 3 waypoint，左臂末端 TF 实测 x −0.393 → −0.345 |
| torso_aim | (0.6,0,1.2)（可达） | ✅ outcome=0：规划 37 点 → 17 waypoint 全部到位 |
| torso_aim | (1.0,0,0.6)（不可达） | ❌ outcome=1：倾角盲区 ray_error=0.065>3cm 判不可达（失败路径按预期返回） |
| base_move | 0.6m | ✅ outcome=0：nav2 实动到达，odom (0,0) → (0.42,0.02) |
| base_move | 0.2m | ⚠️ 假成功：nav2 xy_goal_tolerance 0.25m，0.2m 目标判"立即到达"（增量建议 ≥0.5m） |

三个演示任务的实跑视频（SDK 仿真环境）：

- [导航任务（base_move）演示视频](videos/decision_Nav.mp4)
- [右臂任务（arm_move）演示视频](videos/decision_RightArm.mp4)
- [躯干瞄准（torso_aim）演示视频](videos/decision_Torso.mp4)

#### B\.2\.5 遗留问题

- **真机投入前标定项**：里程计死推比例、SDK +wz 旋转方向复核、位姿 IK 可达性；
- **传感器受限**：SDK 经 TCP 仅回报关节＋底盘状态，相机/雷达/IMU 暂无数据（`/perception/health` 相应 unavailable 属预期）；如需视觉/激光功能需另行接入传感器驱动（A.1.5.2 例程中的 RealSense/YDLidar 等）；
- **"动作成功"≠"真动"**：SDK 只回显目标状态（`getRobotMotion` 返回期望而非实际），判定以仿真界面/真机实动为准，勿只信日志/状态回显；
- **接口能力约束沿用 A.2.4 测试结论**：仅暴露关节空间控制、底盘三模式、夹爪布尔量——直线/圆弧轨迹、terminalJOG 等不可用接口不暴露给上层。


## C\. TR-RD28&&YL-R1D 资料网络位置

本项目资料分散在四个网络位置，按报告章节对应关系列示如下：

| # | 内容 | 网络位置 | 说明 |
|---|------|----------|------|
| 1 | **正文部分**（控制系统框架，YLR1D_Controller） | https://github.com/Zhengshuji/YLR1D_Controller | ROS2 Humble + Gazebo Classic 仿真控制系统框架（12 个功能包），分支 `model_fix`；对应开发报告第 3–13 章 |
| 2 | **附录 B**（真机控制系统，YLR1D_Controller_SDK） | https://github.com/Zhengshuji/YLR1D_Controller_SDK | SDK 封装为 ROS2 节点并接入控制系统框架（robot_interfaces / robot_package / robot_driver / robot_sensors / bringup）；对应开发报告附录 B |
| 3 | **附录 A**（开发成果归档，YLR1D_controller-collection） | https://github.com/Zhengshuji/YLR1D_controller-collection | 《TR-RD28&&YL-R1D机器人系统控制器开发报告》（合并版）及附录 A.2 开发成果（模型处理工具、参数文档、模型流水线、控制验证）；对应开发报告附录 A.2 |
| 4 | **附录 A**（厂家处理前内容，百度网盘） | https://pan.baidu.com/s/1sfX1zpC65LKl5oErayX1CA?pwd=7vf1 （提取码 `7vf1`） | 厂家随设备提供的处理前原始资料（技术说明书、网络配置文档、SDK 包、YL-R1D 应用案例、仿真环境与上位机、设备图片等），需自行提取后使用；对应开发报告附录 A.1 |

## 对应关系说明

- **开发报告第 3–13 章（正文）**：仿真控制系统的分层架构、各功能包设计，代码见位置 #1；
- **开发报告附录 A**：A.1 原始资料（网盘，位置 #4）→ A.2 处理成果（本仓库，位置 #3）；
- **开发报告附录 B**：真机控制系统（SDK 封装与全链路实测），代码见位置 #2。

> 注：位置 #3 与 #4 的内容存在加工前后关系：网盘（#4）为处理前厂家原始资料，本仓库（#3）为在其基础上加工、转换、验证后的成果；仿真软件（TR-Sim/TR-Pad）与大型模型文件不纳入 GitHub 仓库（体积过大），以本地归档或网盘为准。