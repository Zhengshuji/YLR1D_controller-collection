# YLR1D 机器人控制系统

项目主要位于https://github\.com/Zhengshuji/YLR1D\_Controller/tree/model\_fix

# 1 框架结构

主要分为五层：

1. 感知层：获取传感器信息、预处理，完成消息派送，实现高级感知功能

2. 决策层：任务决策

3. 规划层：实现关节空间轨迹规划、运动导航控制，实际中调用SDK包

4. 控制层：下位机实现多闭环控制，实际中SDK中与硬件层合并

5. 硬件层：仿真时配置gazebo参数



被控对象，详见[被控对象数学建模](https://qcnkr8qd7w8a.feishu.cn/wiki/OzYtwg9zei3K6LkJFtecun2jnrh)

# 2 仿真控制系统设计

由于小车底座SDK没有提供太多信息，因此暂时这样写。

## 2\.1 物理层

模拟实现真实机器人效果，主要包含两个方面的内容：

1. 运动学仿真，控制位置。并且这种方法更加契合SDK包。

2. 动力学仿真，控制力、力矩。这个更加真实，但SDK包已经将这部分内容涵盖。

这里采用了xacro模型，便于调整参数。

此外，为了提升仿真效率，需要尽量降低gazebo的开销。（一方面，不需要gazebo界面可视，另一方面，调整gazebo仿真设置）

并且，需要考虑机器人自身部件碰撞产生的影响。（待定）

> 原本模型中：
> 
> 1. 左侧轮，Joint\_LFWheelF\_to\_LFWheel、Joint\_LBWheelF\_to\_LBWheel速度为正时反转，需要调整
> 
> 2. 左臂，Joint\_Body2\_to\_LeftArm1、Joint\_LeftArm2\_to\_LeftArm3、Joint\_LeftArm3\_to\_LeftArm4、Joint\_LeftArm5\_to\_LeftArm6、Joint\_LeftArm6\_to\_LeftArm7，这些关节反向
> 
> 3. 右臂，Joint\_RightArm2\_to\_RightArm3、Joint\_RightArm3\_to\_RightArm4、Joint\_RightArm5\_to\_RightArm6、Joint\_RightArm6\_to\_RightArm7，这些关节反向
> 
> 4. 夹爪有问题，但受初始位置限制，后面再说。
> 
> 另外，SolidWorks导出的urdf，无法更改之前错误的结果。
> 
> 

## 2\.2 控制层

运动控制指令封装、发送。

输出方，考虑与SDK包适配的需求，保证输出与SDK包一致，即将机器人系统拆分为躯干、左臂、右臂、底座四个独立部分，各部分整体控制。

输入方，考虑在机器人系统决策规划是以整体进行，若输入为整体，则需要拆分重组，同时每个控制量应该能够保持。因此，输入是以一个话题、不同配置，实现每个关节的控制。

内部主要包含：

1. 实现闭环PID控制器。**（实际上，可以扩展为任意离散系统模型，这里以PID控制器为例）**

    1. 对于位置控制，直接假设输出量为加速度，系统模型为$G(s)=\frac{1}{s^2}$

    2. 对于速度控制，假设输出量为加速度，系统模型为$G(s)=\frac{1}{s}$

2. 输入、输出以及中间状态变量限幅处理操作

3. 适当对输入量进行调整，以弥补建模过程中产生的问题

    1. 夹爪，使用0、1控制开、合即可。并且，两个夹指只需要任意控制其中一个就行（还是迁移到规划层）

    2. ~~部分关节，由于建模问题，导致Z轴方向恰好相反~~（直接在模型中修改）

改进方案：[控制层改进](https://qcnkr8qd7w8a.feishu.cn/wiki/S75iwS2guimDs5kaV25ceFionSe)，实际上相当于增加了算法层，只不过算法层是嵌入在控制层中的，不与其他层级直接相连。

## 2\.3 转译层

用于在规划层与控制层之间实现连接，这层与上一层之间主要采用Action的方式进行通信。主要包含：

1. 小车控制指令由原本的八关节控制，现在封装固定的移动模式，变为运动方式、运动速度控制，解算得到对应控制量。

2. 夹爪控制，由两关节控制，变为一个布尔量控制

3. 对机械臂控制进行适当处理（待定）

---

## 2\.4 规划层

分为两个部分：小车底座、机械臂移动。这层与上一层之间，主要采用Action的方式进行通信。

## 2\.4\.1 小车底座

生成具体的运动控制指令

1. 移动方式

2. 移动速度

主要依赖nav2

### 2\.4\.2 机械臂

生成可执行的关节空间序列

1. 平滑

2. 避障

3. 动态响应

主要依赖movelt2

## 2\.4 决策层

### 2\.4\.1 小车底座

运动决策

### 2\.4\.2 机械臂

末端决策

### 2\.4\.3 融合决策

小车底座、机械臂怎么联动

躯干、双臂怎么联动

---

## 2\.5 感知层

主要用于处理不同传感器，功能相对分散。有时还需要必要的标定等算法。

### 2\.5\.1 收集信息汇总 （抹除信息来源差别）

1. 机械臂关节位置信息

2. 小车底座关节位置信息、车轮速度信息

3. 雷达

4. 超声波传感器

5. IMU

6. 相机（深度、彩色、红外），主相机与两个手部相机

### 2\.5\.2 感知层感知信息（感知层产生的高级感知功能）

|感知信息|基础信息来源|算法|
|---|---|---|
|机械臂关节速度、加速度、力|机械臂关节位置||
|SLAM|IMU、主相机、雷达、超声波传感器||
|计算机视觉信息|相机||
|里程计|IMU、小车底座信息||

### 2\.5\.3 信息分发

1. 控制层

    1. 机械臂关节位置、速度、加速度、力

    2. 小车底座信息

2. 规划层

    1. nav2：SLAM

    2. movelt2：

3. 决策层



## 3 人机交互界面

## 3\.1 控制层

基于控制层，该人机交互界面的设计，基本与SDK包提供的功能一致，主要包含这些要点：

1. 展示当前各个关节状态

    1. 展示机器人的关节组成、分组情况**（有些内容没有介绍到）**

    2. 展示各个关节角度、速度**（物理量没有明确。并且需不需要单位转换？）**

2. 实现对各个关节的控制**（仅包含SDK的关节空间控制）**

3. rviz2内嵌，展示机器人模型（待定）

## 3\.2 转译层

支持以封装的指令，直接控制小车底座、机械臂。

1. 小车底座：

    1. 移动方式 \-\> 车轮朝向

    2. 移动速度 \-\> 车轮速度

2. 机械臂：

    1. 机械臂编号 \-\> 关节名称组

    2. 机械臂各个关节角度 \-\> 广义坐标向量

    3. 机械臂速度限制 \-\> 具体控制参数

    4. 机械臂夹爪开合 \-\> 直接控制机械夹爪位置

---

## 3\.3 规划层



## 3\.4 决策层



## 3\.5 感知层

1. 展示传感器状态以及传感器观测到的信息；

2. 感知自身各个节点是否正常运行

3. 







